# Render-аудит порта 1.21.11 — мёртвый код / матрицы / устаревшие API

_Сгенерировано multi-agent аудитом: 20 областей × (scan + adversarial verify). Сырых находок: 88; подтверждено: 87; переклассифицировано: 1; отклонено верификаторами: 0; добавлено при верификации: 2._

## Сводка

| Категория | Кол-во |
|---|---|
| Мёртвый render-код (dead) | 63 |
| Проблемы с матрицами (matrix) | 10 |
| Устаревшие вызовы render/GL API (deprecated-api) | 17 |

**По действию:** удалять/чинить сейчас (actionable) — **43**; намеренные заглушки незаконченных фич (deferred) — **47**.
**По уверенности:** high=73, medium=13, low=4.

## Мёртвый render-код (dead) — 63

### ⚙️ Можно удалять/чинить сейчас (actionable) — 38

- **`src/client/java/mchorse/bbs_mod/client/BBSRendering.java:72-73, 203-204`** — `BBSRendering.iris / BBSRendering.sodium` — _write-only-fields_ · conf=high
  - **Доказательство:** private static boolean iris; private static boolean sodium; ... iris = false; sodium = false;
  - **Почему:** Both fields are assigned once in setup() and never read anywhere in the repo (verified by grep: only the declarations and the two false-assignments match; all other 'iris'/'sodium' hits are package-path names). isIrisShadersEnabled()/isIrisShadowPass() hardcode `return false` instead of reading these fields, so the values are inert. Accidental residue of the decoupled Iris/Sodium integration.
  - **Вердикт верификатора:** Re-read confirms. Lines 72-73 declare `private static boolean iris; private static boolean sodium;`. Lines 203-204 (inside setup()) assign `iris = false; sodium = false;`. Repo-wide grep for \biris\b/\bsodium\b returns only these 4 lines plus unrelated package-path hits (utils.iris.ShaderCurves, IrisVertexFormats javadoc). The accessors isIrisShadersEnabled() (line 537) and isIrisShadowPass() (lin
  - **Рекомендация:** Delete fields at lines 72-73 and assignments at lines 203-204. Keep the explanatory comment.
- **`src/client/java/mchorse/bbs_mod/client/BBSRendering.java:548-551`** — `BBSRendering.calculateTangents(float[] t, float[] v, float[] n, float[] u)` — _unused-overload_ · conf=high
  - **Доказательство:** public static float[] calculateTangents(float[] t, float[] v, float[] n, float[] u) {     return t; }
  - **Почему:** The 4-arg overload has zero callers across the whole repo. Both real call sites (CubicVAOBuilderRenderer.java:76 and TextureExtruder.java:205) use the 3-arg overload. The 4-arg version is dead residue of the original (pre-port) tangent API.
  - **Вердикт верификатора:** Re-read confirms the 4-arg overload (lines 548-551) just returns its first arg `t`. Repo-wide grep for calculateTangents shows exactly two call sites: CubicVAOBuilderRenderer.java:76 and TextureExtruder.java:205, both calling the 3-arg overload `calculateTangents(v, n, u)`. The 4-arg overload has zero callers. Dead residue of the pre-port tangent API; safe to delete.
  - **Рекомендация:** Delete the 4-arg overload (lines 548-551).
- **`src/client/java/mchorse/bbs_mod/client/BBSRendering.java:553-556`** — `BBSRendering.calculateTangents(float[] v, float[] n, float[] u)` — _stub-passthrough-tangents_ · conf=high
  - **Доказательство:** public static float[] calculateTangents(float[] v, float[] n, float[] u) {     return v; }
  - **Почему:** This overload is called (CubicVAOBuilderRenderer:76, TextureExtruder:205) but it does not compute tangents — it returns the vertex array `v` unchanged. The result is stored as ModelVAOData.tangents (record field, ModelVAOData.java:3). So every VAO's 'tangents' channel is actually a duplicate of positions; any shader path consuming tangents gets garbage. Since the VAO render path was re-ported via immediate getModelLayer() draw and normal mapping/tangents are not wired, the tangents field is comp
  - **Вердикт верификатора:** Verified the full data chain. The 3-arg overload (lines 553-556) returns `v` unchanged (no tangent computation). Both callers store the result as the `tangents` channel: CubicVAOBuilderRenderer.java:76-78 does `float[] t = calculateTangents(v,n,u); new ModelVAOData(v, n, t, u)`; TextureExtruder.java:205 likewise. ModelVAOData (ModelVAOData.java:3) is the record `(vertices, normals, tangents, texCo
  - **Рекомендация:** Drop the tangents channel from ModelVAOData and remove both calculateTangents call sites (CubicVAOBuilderRenderer:76, TextureExtruder:205) plus this stub. If tangent-space normal mapping is later planned, reintroduce a real computation with a // TODO(1.21.11 render) marker rather than this silent passthrough.
- **`src/client/java/mchorse/bbs_mod/client/BBSShaders.java:480-499`** — `BBSShaders.register(String, VertexFormat, String...)` — _unused-private-register_ · conf=high
  - **Доказательство:** private static RenderPipeline register(String name, VertexFormat format, String... samplers) { ... for (String sampler : samplers) { builder.withSampler(sampler); } return RenderPipelines.register(builder.build()); }
  - **Почему:** This generic varargs pipeline builder has zero callers: every pipeline is built by the specialized registerModel/registerParticles/registerMultilink/registerSubtitles/registerPicker methods instead. It is dead private code (the engine RenderPipelines.register at the other lines is a different method).
  - **Вердикт верификатора:** The private varargs register(String,VertexFormat,String...) helper has zero callers. Every pipeline field (MODEL/MULTILINK/SUBTITLES/PICKER_*/PARTICLES) is built via the specialized registerModel/registerParticles/registerMultilink/registerSubtitles/registerPicker methods. A repo grep for register( shows only the engine RenderPipelines.register(...) (a different method) and unrelated event/proxy r
  - **Рекомендация:** Delete the unused private register(String, VertexFormat, String...) method.
- **`src/client/java/mchorse/bbs_mod/client/BBSShaders.java:176-179`** — `BBSShaders.getModel()` — _unused-pipeline-accessor_ · conf=high
  - **Доказательство:** public static RenderPipeline getModel() { return MODEL; }
  - **Почему:** The getModel() RenderPipeline accessor has no live caller (only a javadoc @link reference in ModelFormRenderer line 370). The MODEL pipeline is consumed exclusively through getModelLayer().
  - **Вердикт верификатора:** Grep for getModel() shows the only reference to BBSShaders.getModel() is a javadoc {@link BBSShaders#getModel()} at ModelFormRenderer.java:370 — not a live call. All other getModel() hits are unrelated methods on IModelInstance/ModelInstance/armature/ModelFormRenderer. The MODEL pipeline is consumed only via getModelLayer(). The accessor returning the raw RenderPipeline is dead.
  - **Рекомендация:** Remove getModel() (keep getModelLayer()).
- **`src/client/java/mchorse/bbs_mod/client/BBSShaders.java:242-250, 252-260, 272-280, 292-300`** — `BBSShaders.getSubtitlesLayer / getPickerPreviewLayer / getPickerBillboardNoShadingLayer / getPickerModelsLayer (+ memoized fields)` — _unused-layer-accessors_ · conf=high
  - **Доказательство:** getSubtitlesLayer, getPickerPreviewLayer, getPickerBillboardNoShadingLayer, getPickerModelsLayer are referenced nowhere as live calls (whole-repo grep finds only comment mentions). picker_preview is drawn via getPickerPreviewProgram() in BBSPickerRenderer.drawHighlight (manual pass), picker_models via getPickerModelsProgram() in BBSPickerRenderer.draw -- both bypass the RenderLayer wrappers because the immediate RenderLayer.draw path cannot carry the custom BBSPicker UBO.
  - **Почему:** These RenderLayer accessors and their memoized fields are unreachable. The picker effects that exist (picker_preview, picker_models) are dispatched through the *Program accessors + BBSPickerRenderer manual render passes (the only way to bind the custom BBSPicker std140 UBO); the layer wrappers can never carry that UBO, so they are residual dead code.
  - **Вердикт верификатора:** Whole-repo grep of BBSShaders.getXxxLayer calls: getSubtitlesLayer -> only a comment at UISubtitleRenderer:181; getPickerModelsLayer -> only a comment at LabelFormRenderer:137; getPickerPreviewLayer and getPickerBillboardNoShadingLayer -> no references at all (only their own definitions). picker_preview is dispatched via getPickerPreviewProgram() in BBSPickerRenderer:465; picker_models via getPick
  - **Рекомендация:** Delete getSubtitlesLayer, getPickerPreviewLayer, getPickerBillboardNoShadingLayer, getPickerModelsLayer and their backing fields (subtitlesLayer, pickerPreviewLayer, pickerBillboardNoShadingLayer, pickerModelsLayer).
- **`src/client/java/mchorse/bbs_mod/client/BBSShaders.java:232-240`** — `BBSShaders.getMultilinkLayer` — _uncalled-render-layer-getter_ · conf=high _(added in verify)_
  - **Доказательство:** public static RenderLayer getMultilinkLayer() { if (multiLinkLayer == null) { multiLinkLayer = layer("multilink", MULTILINK, false); } return multiLinkLayer; }  — grep for getMultilinkLayer across the whole tree returns only this definition (BBSShaders.java:232) and one mention inside TODO comment TEXT at UIMultiLinkEditor.java:224; there is no real caller.
  - **Рекомендация:** Dead-by-transitivity: the only consumer of the multilink pipeline (UIMultiLinkEditor) goes through the ignored-supplier texturedBox bridge and never invokes getMultilinkLayer(). Delete getMultilinkLayer() and its multiLinkLayer field, or revive it together with the deferred 'revive GUI custom shaders' work. The companion getMultilinkProgram() (BBSShaders.java:181) is also effectively dead-by-transitivity since its sole caller's result (UIMultiLinkEditor.java:235) is discarded by the @Deprecated texturedBox(Supplier,...) bridge.
- **`src/client/java/mchorse/bbs_mod/client/render/picker/BBSPickerRenderer.java:197-206`** — `BBSPickerRenderer.bind(RenderPass)` — _uncalled-public-method_ · conf=high
  - **Доказательство:** public static void bind(RenderPass pass) {     GpuDevice device = RenderSystem.getDevice();     pass.setUniform(BBSShaders.PICKER_UNIFORM, writeUniform(device, device.createCommandEncoder())); } // javadoc: "Use when driving a custom picker pass by hand; {@link #draw} calls it."
  - **Почему:** No caller anywhere in the repo (grep for `BBSPickerRenderer.bind`/`.bind(` over src/client returns only the definition). The javadoc claim that draw() calls it is false: draw() (line 233/275), drawColorId() and drawHighlight() (line 477/516) all bind the BBSPicker UBO inline via writeUniform(...) + pass.setUniform(BBSShaders.PICKER_UNIFORM, ...) directly and never invoke bind(). It is dead residue from an earlier draft of the manual-pass design.
  - **Вердикт верификатора:** Confirmed dead. Grep over src for `BBSPickerRenderer.bind`/`.bind(` returns only this definition (every other `.bind(` hit is an unrelated `texture.bind()` or a `ShaderProgram.bind` javadoc mention). The javadoc claim '{@link #draw} calls it' is verifiably FALSE: draw() (lines 233/275) inlines `writeUniform(...)` then `pass.setUniform(BBSShaders.PICKER_UNIFORM, ...)` directly and never invokes bin
  - **Рекомендация:** Delete bind(RenderPass) as accidental dead residue (also note: its 'draw calls it' javadoc is false). If a hand-driven external picker pass is genuinely wanted later, re-add it then with a correct doc.
- **`src/client/java/mchorse/bbs_mod/client/render/picker/BBSPickerRenderer.java:161-164`** — `BBSPickerRenderer.getTarget()` — _uncalled-getter_ · conf=high
  - **Доказательство:** public static int getTarget() {     return target; }
  - **Почему:** No callers in the repo (grep `getTarget()` shows only an unrelated ParticleEmitter.setTarget and UIPickableFormRenderer.setTarget; this static accessor on BBSPickerRenderer is never read). setTarget(int) is used (FormRenderer.setupTarget, drawHighlight), but the reader is never consulted — the target is uploaded directly inside writeUniform from the static field.
  - **Вердикт верификатора:** Confirmed dead. Grep for `BBSPickerRenderer.getTarget` returns nothing; grep for `getTarget()` returns only this definition (the other target-related symbols in the repo are an unrelated ParticleEmitter/UIPickableFormRenderer setTarget). The static `target` field is written by setTarget (FormRenderer.setupTarget line 276, drawHighlight line 459) and consumed directly inside writeUniform (line 191)
  - **Рекомендация:** Remove getTarget() as dead residue. Low urgency (harmless debug-style accessor) but currently unused.
- **`src/client/java/mchorse/bbs_mod/cubic/ModelInstance.java:280-289, 380-391`** — `ModelInstance.isVAORendered / ModelInstance.render` — _always-false-vao-branch_ · conf=high
  - **Доказательство:** public boolean isVAORendered() { ... return this.model instanceof BOBJModel; } ... if (this.model instanceof Model model) {     boolean isVao = this.isVAORendered();     CubicCubeRenderer renderProcessor = isVao ? new CubicVAORenderer(this, ...) : new CubicCubeRenderer(...);     ... if (isVao) { CubicRenderer.processRenderModel(renderProcessor, null, stack, model); } else { ... }
  - **Почему:** isVAORendered() returns `model instanceof BOBJModel`, but isVao is evaluated only inside the `if (this.model instanceof Model)` branch. A model can never be both Model and BOBJModel, so isVao is unconditionally false here. The `new CubicVAORenderer(...)` true-branch (and the whole cubic VAO draw via CubicRenderer.processRenderModel with isVao geometry) is therefore unreachable. The method's own comment confirms intent: 'Keep cubic on the immediate path (no per-group ModelVAO build/retain needed)
  - **Вердикт верификатора:** Verified directly. isVAORendered() (line 288) returns `this.model instanceof BOBJModel`. Its only code callsite is line 380, inside `if (this.model instanceof Model model)` (line 378). A reference can never be both Model and BOBJModel, so isVao at line 380 is unconditionally false. Therefore the `new CubicVAORenderer(...)` ternary branch (line 383) and the `if (isVao) CubicRenderer.processRenderMo
  - **Рекомендация:** Collapse the dead Model branch to always use the immediate CubicCubeRenderer path; remove the isVao ternary and the CubicVAORenderer instantiation. This transitively makes CubicVAORenderer (and its ModelVAORenderer/ModelVAO read of getVaos) dead for the cubic path.
- **`src/client/java/mchorse/bbs_mod/cubic/ModelInstance.java:85, 118-121, 258-278, 291-299`** — `ModelInstance.vaos / getVaos / setup / delete` — _unused-vao-build_ · conf=high
  - **Доказательство:** private Map<ModelGroup, ModelVAO> vaos = new HashMap<>(); ... if (this.model instanceof Model model && !this.onCpu) {     MinecraftClient.getInstance().execute(() -> {         CubicRenderer.processRenderModel(new CubicVAOBuilderRenderer(this.vaos), null, new MatrixStack(), model);     }); }
  - **Почему:** setup() builds per-group ModelVAO/ModelVAOData objects into this.vaos for every non-onCpu cubic Model. The only reader of this.vaos is CubicVAORenderer (this.model.getVaos().get(group) at CubicVAORenderer.java:31), which is the never-instantiated true-branch from the finding above. So the entire VAO build is dead compute/allocation: the map is populated and then only iterated in delete() to call ModelVAO.delete() (itself a documented no-op). getVaos() has no other callers across the repo.
  - **Вердикт верификатора:** Verified. setup() (lines 271-277) builds per-group ModelVAO objects into this.vaos via CubicVAOBuilderRenderer for every non-onCpu cubic Model with no shape keys. setup() is genuinely invoked: ModelManager holds ModelInstance values (ModelManager.java:40) and calls model.setup() at ModelManager.java:135, so the build actually runs. The only reader of this.vaos is getVaos() (line 118), whose sole c
  - **Рекомендация:** Remove the setup() CubicVAOBuilderRenderer build (lines 271-277), the vaos field (85), getVaos() (118-121), and the delete() loop (293-296) — once the dead VAO branch in finding 1 is collapsed.
- **`src/client/java/mchorse/bbs_mod/cubic/ModelInstance.java:38, 53, 376`** — `ModelInstance.render (Supplier<ShaderProgram> program)` — _unused-shader-param_ · conf=high
  - **Доказательство:** import net.minecraft.client.gl.ShaderProgram; ... public void render(MatrixStack stack, Supplier<ShaderProgram> program, Color color, int light, int overlay, StencilMap stencilMap, ShapeKeys keys) { ... }
  - **Почему:** The `program` parameter is never referenced anywhere in the method body (the body uses BBSShaders.getPickerModelsProgram(), BBSShaders.getModelLayer() and RenderLayers.entityCutoutNoCull(...) directly). The sole caller ModelFormRenderer.renderModel passes the supplier from getMainShader(), which always returns `() -> null` (ModelFormRenderer.java:377-380). So this is a lingering ShaderProgram reference kept from the 1.21.1 imperative-shader API that no longer exists on 1.21.11.
  - **Вердикт верификатора:** Verified. The `program` parameter (line 376) is never referenced in the render body (lines 377-443); grep for `\bprogram\b` in the file shows it only on the signature line 376. The body uses BBSShaders.getPickerModelsProgram(), BBSShaders.getModelLayer(), and RenderLayers.entityCutoutNoCull(...) directly. The sole caller ModelFormRenderer.renderModel (line 413) passes getMainShader(model), which a
  - **Рекомендация:** Drop the `Supplier<ShaderProgram> program` parameter and the ShaderProgram import (line 38) from ModelInstance.render, and update the single caller at ModelFormRenderer.java:413 (also removing getMainShader plumbing if it becomes unused).
- **`src/client/java/mchorse/bbs_mod/cubic/ModelInstance.java:87`** — `ModelInstance.lastBaseTransform` — _write-only-matrix-field_ · conf=high
  - **Доказательство:** public transient Matrix4f lastBaseTransform;
  - **Почему:** This Matrix4f is only ever written (ModelFormRenderer.java:487 `model.lastBaseTransform = baseTransform;`) and never read anywhere in the repo. It captures the world base transform per render but nothing consumes it, so the stored matrix is dead.
  - **Вердикт верификатора:** Verified by repo-wide grep for 'lastBaseTransform': exactly two hits — the declaration at ModelInstance.java:87 and the write at ModelFormRenderer.java:487 (`model.lastBaseTransform = baseTransform;`). No read anywhere. The field captures the world base transform per render but nothing consumes it, so the stored Matrix4f is dead. Raising confidence from the finder's medium to high since the grep i
  - **Рекомендация:** Remove the field (line 87) and its write at ModelFormRenderer.java:487, unless intentionally retained as a future IK/physics/picking hook — in which case add a TODO(1.21.11 render) marker.
- **`src/client/java/mchorse/bbs_mod/cubic/render/CubicAxisRenderer.java:10-44`** — `CubicAxisRenderer` — _unwired-renderer-class_ · conf=high
  - **Доказательство:** public class CubicAxisRenderer implements ICubicRenderer { ... builder.vertex(matrix, this.vector.x, this.vector.y, this.vector.z).color(1, 0, 0, 1); ... }
  - **Почему:** This ICubicRenderer (a per-group debug XYZ-axis gizmo drawer) is never instantiated anywhere. A repo-wide grep for 'new CubicAxisRenderer' returns zero hits; the only match for the symbol is its own declaration. git grep on master and 1.21.1 confirms it has had no callers there either, so it is long-standing orphaned debug code that survived the port without being wired in. It also emits geometry only by appending raw vertices to a BufferBuilder with no begin()/draw() of its own, so even if reac
  - **Вердикт верификатора:** Re-grepped the whole repo: the only occurrence of the symbol 'CubicAxisRenderer' is its own class declaration at line 10; 'new CubicAxisRenderer' returns zero hits. It is an ICubicRenderer (a debug per-group XYZ-axis gizmo) that is never instantiated anywhere, so it is unreachable dead code with no TODO marker — accidental orphan, not a deferred port stub. Confirmed it only appends raw vertices to
  - **Рекомендация:** Delete CubicAxisRenderer.java. If an axis-gizmo overlay is later wanted, rebuild it on the in-world immediate path (Tessellator.begin + a POSITION_COLOR RenderLayer.draw) like ModelIKDebug/Draw rather than as a free-floating ICubicRenderer.
- **`src/client/java/mchorse/bbs_mod/cubic/render/CubicVAORenderer.java:17-57`** — `CubicVAORenderer` — _unreachable-instantiation_ · conf=high
  - **Доказательство:** ModelInstance.render(): if (this.model instanceof Model model) { boolean isVao = this.isVAORendered(); CubicCubeRenderer renderProcessor = isVao ? new CubicVAORenderer(this, light, overlay, stencilMap, keys) : new CubicCubeRenderer(...); }  and  isVAORendered(){ return this.model instanceof BOBJModel; }
  - **Почему:** CubicVAORenderer's only construction site is ModelInstance.render line 383, guarded by isVao = isVAORendered(). isVAORendered() returns 'this.model instanceof BOBJModel'. But that construction is inside 'if (this.model instanceof Model model)', and Model (implements IMapSerializable, IModel) and BOBJModel (implements IModel) are unrelated sibling classes — neither extends the other. So within the cubic-Model branch isVao is always false and the new CubicVAORenderer(...) arm is never taken. The c
  - **Вердикт верификатора:** Verified the sole construction site: ModelInstance.render line 383 'new CubicVAORenderer(this, ...)', inside 'if (this.model instanceof Model model)' (line 378), guarded by isVao = isVAORendered() (line 380). isVAORendered() (line 288) returns 'this.model instanceof BOBJModel'. Class hierarchy confirmed by grep: 'Model implements IMapSerializable, IModel' (src/main/.../data/model/Model.java:26) an
  - **Рекомендация:** Delete CubicVAORenderer.java and simplify ModelInstance.render to always use CubicCubeRenderer in the Model branch (drop the dead isVao ternary). BOBJModel uses a separate VAO path (BOBJModelVAO / ModelVAORenderer), not this class.
- **`src/client/java/mchorse/bbs_mod/cubic/render/vao/Attributes.java:6-22`** — `Attributes (POSITION/COLOR/TEXTURE_UV/OVERLAY_UV/LIGHTMAP_UV/NORMAL/MID_TEXTURE_UV/TANGENTS)` — _orphan-vao-attribute-class_ · conf=high
  - **Доказательство:** public class Attributes {     public static final int POSITION = 0;     ...     public static final int NORMAL = 5;     public static final int MID_TEXTURE_UV = 8;     public static final int TANGENTS = 9; }
  - **Почему:** This class only names raw-GL vertex-attribute slot indices for glVertexAttribPointer/glEnableVertexAttribArray binding of the old hand-rolled VAO. Raw-GL VAO is gone in 1.21.5+ (glGenVertexArrays/glVertexAttribPointer/glDrawArrays all removed) and the VAO meshes now emit through a BufferBuilder + RenderLayer. Grep across the whole repo finds ZERO references to any Attributes.* constant and ZERO imports of this class; the only glVertexAttrib* token left in src is a doc comment in ModelVAO.java. T
  - **Вердикт верификатора:** Verified by reading the file and grepping the whole repo. Attributes only declares raw-GL vertex-attribute slot indices (for glVertexAttribPointer/glEnableVertexAttribArray of the removed hand-rolled VAO). Grep for 'Attributes.POSITION|COLOR|...|TANGENTS' returns ZERO matches; grep for any import of cubic.render.vao.Attributes returns ZERO matches. The only surviving glVertexAttrib* token in src i
  - **Рекомендация:** Delete Attributes.java entirely (no callers, no imports).
- **`src/client/java/mchorse/bbs_mod/cubic/render/vao/ModelVAOData.java:3-4`** — `ModelVAOData.tangents (record component)` — _unused-tangent-record-component_ · conf=high
  - **Доказательство:** public record ModelVAOData(float[] vertices, float[] normals, float[] tangents, float[] texCoords) {}
  - **Почему:** The 'tangents' component is constructed (CubicVAOBuilderRenderer.java:78 and TextureExtruder.java:207 pass a 't' array) but its accessor data.tangents() is never read anywhere in the repo (grep for 'tangents' shows only writers/constructors, no reads). The live draw path writes format POSITION_COLOR_TEXTURE_OVERLAY_LIGHT_NORMAL (ModelVAO.writeImmediate / BOBJModelVAO.render) which has no tangent attribute. Worse, the producer BBSRendering.calculateTangents(v,n,u) is now a stub that returns its f
  - **Вердикт верификатора:** Confirmed. The 'tangents' component is written at CubicVAOBuilderRenderer.java:78 and TextureExtruder.java:207 (both pass a 't' array), but its accessor data.tangents() is never read: grep for '.tangents()' returns ZERO matches, and grep for 'tangents' across the repo matches ONLY the record declaration plus the two writer sites. Both live draw paths (ModelVAO.writeImmediate, BOBJModelVAO.render) 
  - **Рекомендация:** Drop the 'tangents' component from ModelVAOData and the dead 't'-array computation feeding it (CubicVAOBuilderRenderer.java:76,78 and TextureExtruder.java:205,207). The calculateTangents stub itself is in BBSRendering.java, outside this work unit's file scope.
- **`src/client/java/mchorse/bbs_mod/film/BaseFilmController.java:3`** — `BaseFilmController (import com.mojang.blaze3d.systems.RenderSystem)` — _unused-import_ · conf=high
  - **Доказательство:** import com.mojang.blaze3d.systems.RenderSystem;  // line 3 — every remaining RenderSystem token in this file is inside a comment, e.g. /* TODO(1.21.11 render): depth-test state now lives in the RenderPipeline/RenderLayer; removed RenderSystem.enableDepthTest() */ (lines 214, 238, 894) and /* ... removed RenderSystem.disableCull()/enableCull() */ (lines 475, 485)
  - **Почему:** In the 1.21.1 original this class called RenderSystem.enableDepthTest(), RenderSystem.getProjectionMatrix(), RenderSystem.disableCull()/enableCull() etc. The 1.21.11 port deleted all of those calls (the GL state now lives in RenderPipeline/RenderLayer) and replaced them with TODO comments, but the import line was left behind. grep confirms the only non-comment occurrence of 'RenderSystem' in the file is the import itself — it resolves to nothing used.
  - **Вердикт верификатора:** Grep over the whole file shows 'RenderSystem' appears on lines 3 (the import), 170, 214, 238, 475, 485, 894. I read line 170 and the surrounding block (165-176): it is a multi-line comment explaining the 1.21.1 global-modelview behavior. Lines 214/238/894 are TODO comments noting depth-test state moved to RenderPipeline/RenderLayer (removed RenderSystem.enableDepthTest()); lines 475/485 are TODO c
  - **Рекомендация:** Delete the dead import line `import com.mojang.blaze3d.systems.RenderSystem;` (line 3).
- **`src/client/java/mchorse/bbs_mod/film/Films.java:3`** — `Films (import com.mojang.blaze3d.systems.RenderSystem)` — _unused-import_ · conf=high
  - **Доказательство:** import com.mojang.blaze3d.systems.RenderSystem;  // line 3 — the only other RenderSystem token is the comment at line 296: /* TODO(1.21.11 render): depth-test state now lives in the RenderPipeline/RenderLayer; removed RenderSystem.enableDepthTest() */
  - **Почему:** The 1.21.1 original Films.render() called RenderSystem.enableDepthTest()/disableDepthTest(). The port removed both calls (replaced with a single TODO comment) but kept the import. No live RenderSystem reference remains, so the import is dead residue.
  - **Вердикт верификатора:** Grep over the whole file shows 'RenderSystem' appears only on line 3 (the import) and line 296, which is a TODO comment: '/* TODO(1.21.11 render): depth-test state now lives in the RenderPipeline/RenderLayer; removed RenderSystem.enableDepthTest() */'. There is no executable use of RenderSystem anywhere in the file, so the import is dead residue safe to delete now.
  - **Рекомендация:** Delete the dead import line `import com.mojang.blaze3d.systems.RenderSystem;` (line 3).
- **`src/client/java/mchorse/bbs_mod/forms/renderers/FormRenderer.java:270-277`** — `FormRenderer.setupTarget(FormRenderingContext, ShaderProgram)` — _unused-method-param_ · conf=high
  - **Доказательство:** protected void setupTarget(FormRenderingContext context, ShaderProgram program) {     /* ... The legacy ShaderProgram param is unused (picker programs are RenderPipelines now). */     BBSPickerRenderer.setTarget(context.getPickingIndex()); }
  - **Почему:** The ShaderProgram program parameter is never read; the method only forwards the picking index to BBSPickerRenderer.setTarget. Its caller getShader passes picking.get(), which for every current caller is a () -> null supplier (ModelFormRenderer.getMainShader returns () -> null), so the argument is always null anyway. The parameter is dead residue from the 1.21.1 program.getUniform("Target").set(...) signature.
  - **Вердикт верификатора:** Confirmed. setupTarget (lines 270-277) never reads its `ShaderProgram program` parameter; it only calls `BBSPickerRenderer.setTarget(context.getPickingIndex())` (line 276), and the comment at 272-275 explicitly states 'The legacy ShaderProgram param is unused (picker programs are RenderPipelines now)'. Its sole live caller is getShader (line 262) `this.setupTarget(context, picking.get())`, and the
  - **Рекомендация:** Drop the ShaderProgram parameter from setupTarget (and the Supplier<ShaderProgram> picking arg threading in getShader) so the picker-index path no longer references the removed-API ShaderProgram type.
- **`src/client/java/mchorse/bbs_mod/forms/renderers/MobFormRenderer.java:308-311`** — `MobFormRenderer.BooleanHolder` — _unused-private-class_ · conf=high
  - **Доказательство:** private static class BooleanHolder { public boolean bool; }  -- repo-wide grep for BooleanHolder finds only this definition; never instantiated or referenced.
  - **Почему:** Leftover helper class with no construction or usage anywhere. Accidental dead residue (not feature-gated, no TODO).
  - **Вердикт верификатора:** Verified: BooleanHolder defined at 308-311 (private static class with a single public boolean field). Repo-wide grep for BooleanHolder finds only this definition; never instantiated or referenced anywhere. No feature gate, no TODO. Accidental dead residue, intentional=false, safe to delete.
  - **Рекомендация:** Delete the BooleanHolder inner class.
- **`src/client/java/mchorse/bbs_mod/forms/renderers/utils/RecolorVertexConsumer.java:10`** — `RecolorVertexConsumer.newColor` — _write-only-static-field_ · conf=high
  - **Доказательство:** public static Color newColor;  // only ever assigned (= null) in CustomVertexConsumerProvider.setSubstitute; never read anywhere
  - **Почему:** The static Color newColor field is write-only: the sole reference outside its declaration is CustomVertexConsumerProvider.setSubstitute setting RecolorVertexConsumer.newColor = null. It is never read; the recolor logic uses the instance field this.color. Dead field (pre-existing in master, carried into the port).
  - **Вердикт верификатора:** Confirmed by grep across src. `public static Color newColor;` (line 10) is referenced exactly once outside its declaration: CustomVertexConsumerProvider.java:48 `RecolorVertexConsumer.newColor = null;` — a write only. It is never read; the recolor logic in color(int,int,int,int) (lines 34-42) uses the instance field `this.color`. The other 'newColor' grep hits are unrelated local variables in VoxR
  - **Рекомендация:** Remove the static field and the `RecolorVertexConsumer.newColor = null;` line in CustomVertexConsumerProvider.setSubstitute.
- **`src/client/java/mchorse/bbs_mod/graphics/Draw.java:202-214`** — `Draw.fillTexturedNormalQuad` — _unused-helper_ · conf=high
  - **Доказательство:** public static void fillTexturedNormalQuad(BufferBuilder builder, MatrixStack stack, float x1, ... float nx, float ny, float nz) { Matrix4f matrix4f = stack.peek().getPositionMatrix(); builder.vertex(matrix4f, x2, y2, z2).texture(u1, v2).color(r, g, b, a).normal(nx, ny, nz); ... }
  - **Почему:** This POSITION_TEXTURE_COLOR_NORMAL quad builder has zero callers across the whole repo. `git grep fillTexturedNormalQuad` returns only its own declaration here and an UNRELATED same-named method in graphics/texture/TextureExtruder.java (different class, different signature taking List<Float> instead of BufferBuilder, and that one IS called internally). No other code references Draw.fillTexturedNormalQuad. It is also the only Draw method emitting texture()/normal() — none of the registered Draw p
  - **Вердикт верификатора:** Verified by reading lines 202-214 and a repo-wide grep for 'fillTexturedNormalQuad'. The only other hits are in graphics/texture/TextureExtruder.java, which declares a SEPARATE same-named method with a completely different signature (List<Float> vertices/normals/uvs instead of BufferBuilder+MatrixStack) and is called 6 times internally within that file (lines 171,180,224,236,248,261). Draw.fillTex
  - **Рекомендация:** Delete Draw.fillTexturedNormalQuad (lines 202-214). Dead residue, safe to remove now; does not implement any deferred feature.
- **`src/client/java/mchorse/bbs_mod/graphics/line/Line.java:79-87`** — `Line.getClosest / Line.getFurthest` — _unused-private-methods_ · conf=high
  - **Доказательство:** private Vector2f getClosest(Vector2f a, Vector2f b, Vector2f ref) {     return ref.distanceSquared(a) < ref.distanceSquared(b) ? a : b; } private Vector2f getFurthest(Vector2f a, Vector2f b, Vector2f ref) {     return ref.distanceSquared(a) > ref.distanceSquared(b) ? a : b; }
  - **Почему:** Dead residue: both methods are private and have ZERO callers. A repo-wide grep for getClosest/getFurthest returns only these two declarations (the other getClosest hits are unrelated methods in KeyframeSegment, UIModelBlockPanel and UIReplaysEditorUtils). They are not invoked inside Line.java either — Line.build() does its perpendicular/miter math inline and never references them. They are vestigial helpers from an older line-joining algorithm; nothing reaches them at runtime.
  - **Вердикт верификатора:** Both methods are private with the signature (Vector2f, Vector2f, Vector2f). A repo-wide grep for getClosest/getFurthest returns only these two declarations plus unrelated symbols: KeyframeSegment.getClosest() (no-arg), UIReplaysEditorUtils.getClosestKeyframe, and UIModelBlockPanel.getClosestObject — none of which call Line's methods. Inside Line.java, build() (lines 35-77) computes perpendicular/m
  - **Рекомендация:** Delete both private methods. Removal is safe (private, no callers) and cannot break external code. Keep the Vector2f import — build() and add() still use it.
- **`src/client/java/mchorse/bbs_mod/mixin/client/EntityRendererDispatcherInvoker.java:15-20`** — `EntityRendererDispatcherInvoker.bbs$renderShadow` — _mixin-not-registered_ · conf=high
  - **Доказательство:** @Mixin(EntityRenderManager.class) public interface EntityRendererDispatcherInvoker {     @Invoker("renderShadow")     public static void bbs$renderShadow(MatrixStack matrices, VertexConsumerProvider vertexConsumers, Entity entity, float opacity, float tickDelta, WorldView world, float radius)     {}
  - **Почему:** The file is NOT listed in src/client/resources/bbs.client.mixins.json, so the @Invoker is an inert string and the mixin is never applied. The method body is an empty stub `{}` and a grep of the whole repo shows bbs$renderShadow has ZERO callers. EntityRenderManager has no renderShadow method on 1.21.11, so it could never be wired anyway. The only consumer of the shadow concept, ModelBlockEntityRenderer.renderShadow, is itself fully neutralized (push/pop only) and references this invoker only in 
  - **Вердикт верификатора:** Verified: the file is NOT in bbs.client.mixins.json (the `client` array lines 5-30 lists no EntityRendererDispatcherInvoker), so the @Invoker("renderShadow") is an inert string and the mixin is never applied. The @Invoker body is an empty `{}` stub (line 20). Grep for bbs$renderShadow yields ZERO real callers: the only two hits outside the file itself are (a) line 14 of its own comment and (b) Mod
  - **Рекомендация:** Delete the file. It is unregistered, has zero callers, has an empty stub body, and targets a method (renderShadow) that no longer exists on EntityRenderManager. The shadow re-port is already tracked by ModelBlockEntityRenderer.renderShadow's TODO (lines 71-76).
- **`src/client/java/mchorse/bbs_mod/ui/film/controller/UIFilmController.java:17, 19, 20, 90, 91`** — `UIFilmController (imports GL11, RenderSystem, VertexSorter, GlUniform, ShaderProgram)` — _unused-removed-render-api-imports_ · conf=high
  - **Доказательство:** import org.lwjgl.opengl.GL11; import com.mojang.blaze3d.systems.RenderSystem; import com.mojang.blaze3d.systems.VertexSorter; import net.minecraft.client.gl.GlUniform; import net.minecraft.client.gl.ShaderProgram;  -- none of these symbols are referenced in code; RenderSystem appears only inside TODO comments, the other four not at all.
  - **Почему:** Leftover imports from the 1.21.1 picking/render path. GlUniform and ShaderProgram in particular are the removed loose-uniform / ShaderProgram.bind() API family that no longer exists meaningfully on 1.21.11; RenderSystem state calls were all migrated to RenderPipeline/RenderLayer (their former call sites are now comment stubs). They are pure dead residue.
  - **Вердикт верификатора:** Verified by grep: GL11 (line 17), RenderSystem (line 19), VertexSorter (line 20), GlUniform (line 90), ShaderProgram (line 91) appear only on their import lines. RenderSystem additionally appears inside TODO comments (lines 1224, 1229, 1245, 1326, 1371, 1401) but never as an actual call; the other four have zero further occurrences. All five are unused dead imports — leftovers from the 1.21.1 pick
  - **Рекомендация:** Remove the five unused imports. When the picking projection/depth state is revived it will use the new RenderPipeline foundation, not these symbols.
- **`src/client/java/mchorse/bbs_mod/ui/film/UIFilmPanel.java:147 (decl); referenced 3092 (comment only)`** — `UIFilmPanel.lastView` — _unused-field_ · conf=high
  - **Доказательство:** public final Matrix4f lastView = new Matrix4f();  -- grep across src/client shows it is ONLY mentioned in the stub TODO comment at UIFilmPanel.java:3092 and the explanatory comment at UIFilmPreview.java:348/351; it is never written and never read.
  - **Почему:** In 1.21.1 lastView held the captured WorldRenderContext.positionMatrix for the world-pick ray. The capture is stubbed (renderInWorld at 3085-3098 writes nothing) and the click ray is now reconstructed directly from the world-camera state in UIFilmPreview.render (camera.updateView()). Unlike its sibling lastProjection (still consumed via UIFilmController.getGizmoProjection -> GizmoInteraction), lastView has no remaining reader, so it is dead by transitivity.
  - **Вердикт верификатора:** Verified by grep across src/. 'lastView' appears at UIFilmPanel.java:147 (declaration) and only in comments thereafter (UIFilmPanel.java:3092, UIFilmPreview.java:348/351). It is never written and never read in code. Contrast with its sibling lastProjection (UIFilmPanel.java:148), which IS read at UIFilmController.java:660 (return this.panel.lastProjection) feeding the gizmo projection path — so la
  - **Рекомендация:** Delete the lastView field. Keep lastProjection (still wired into the gizmo path via UIFilmController.getGizmoProjection, just identity until picking is finished).
- **`src/client/java/mchorse/bbs_mod/ui/film/UIFilmPanel.java:3`** — `UIFilmPanel (import RenderSystem)` — _unused-removed-render-api-import_ · conf=high
  - **Доказательство:** import com.mojang.blaze3d.systems.RenderSystem;  -- only referenced in the renderInWorld TODO comment (line 3091); no actual RenderSystem.* call exists in the file.
  - **Почему:** renderInWorld's matrix capture is stubbed (lines 3085-3095), so the RenderSystem.getProjectionMatrix call it used was removed; the import is now dead.
  - **Вердикт верификатора:** Verified by grep: RenderSystem appears at line 3 (import) and only in the renderInWorld TODO comment (line 3091); no RenderSystem.* call exists in the file. The lastProjection/lastView matrix capture in renderInWorld is stubbed, so the former RenderSystem.getProjectionMatrix call was removed. Dead import. Bumping to high — grep is unambiguous.
  - **Рекомендация:** Remove the unused RenderSystem import (re-add when the lastProjection capture is implemented against the new foundation).
- **`src/client/java/mchorse/bbs_mod/ui/film/UIFilmPreview.java:3`** — `UIFilmPreview (import RenderSystem)` — _unused-removed-render-api-import_ · conf=high
  - **Доказательство:** import com.mojang.blaze3d.systems.RenderSystem;  -- RenderSystem appears only in TODO/explanatory comments (lines 348, 473, 474), never in code.
  - **Почему:** renderCursor() is fully stubbed (lines 468-476) and no longer calls RenderSystem.applyModelViewMatrix/renderCrosshair; the import is dead residue of the removed crosshair gizmo path.
  - **Вердикт верификатора:** Verified by grep: RenderSystem appears at line 3 (import) and only inside TODO/explanatory comments (lines 348, 473, 474), never as a code call. renderCursor and the world-pick capture are stubbed, so the former RenderSystem.applyModelViewMatrix/renderCrosshair/getProjectionMatrix call sites are gone. Dead import. Bumping to high — grep shows no code reference.
  - **Рекомендация:** Remove the unused RenderSystem import.
- **`src/client/java/mchorse/bbs_mod/ui/film/UISubtitleRenderer.java:67`** — `UISubtitleRenderer.renderSubtitles (local 'ortho')` — _unused-local-matrix_ · conf=high
  - **Доказательство:** Matrix4f ortho = new Matrix4f().ortho(0, width, height, 0, -100, 100);  -- 'ortho' is never read anywhere else in the method/file.
  - **Почему:** Residue of the removed projection swap. The old subtitle compositing set this ortho as the screen projection via RenderSystem.setProjectionMatrix(ortho); that path is now GPU-owned and stubbed (see the TODOs at lines 59-60, 125-126, 167-169). The Matrix4f is still allocated and computed every subtitle frame but consumed by nothing.
  - **Вердикт верификатора:** Verified by grep: 'ortho' appears as code only at line 67 (Matrix4f ortho = new Matrix4f().ortho(...)); all other occurrences (lines 125, 168) are TODO comments. The local is allocated and computed every subtitle frame but never read. It is residue of the removed RenderSystem.setProjectionMatrix(ortho) projection swap (now stubbed per the TODOs at 59-60, 125-126, 167-169). Accidental dead residue,
  - **Рекомендация:** Remove the dead 'ortho' local; also drop the org.joml.Matrix4f import only if nothing else needs it (note the import is still referenced inside comments, but those do not require it). Recompute ortho inside the projection-restore work when that deferred feature is revived.
- **`src/client/java/mchorse/bbs_mod/ui/framework/elements/input/color/UIColorPicker.java:109-128 (pipeline/layer 85-107; call site 736-747)`** — `UIColorPicker.renderAlphaPreviewQuad / ALPHA_TRIANGLES / getAlphaTrianglesLayer` — _dropped-immediate-draw-2phase-gui_ · conf=high _(reclassified)_
  - **Доказательство:** BufferBuilder builder = Tessellator.getInstance().begin(VertexFormat.DrawMode.TRIANGLES, VertexFormats.POSITION_COLOR); ... BuiltBuffer built = builder.endNullable(); if (built != null) { getAlphaTrianglesLayer().draw(built); }  — invoked from render()->renderRect() during the GUI phase.
  - **Почему:** This is the two-phase-GUI dropped-immediate-draw trap. renderAlphaPreviewQuad runs inside the UIColorPicker.render() recording window (render -> renderRect, when editAlpha) and issues a self-made Tessellator.begin -> RenderLayer.draw(BuiltBuffer). The 1.21.6+ GUI only composites what is recorded into the live DrawContext GuiRenderState; an immediate RenderLayer.draw targets an un-composited state and is overpainted/dropped. Batcher2D itself documents this exact hazard and deliberately AVOIDS it 
  - **Вердикт верификатора:** Code confirmed verbatim: renderAlphaPreviewQuad (109-128) does Tessellator.getInstance().begin(TRIANGLES, POSITION_COLOR) -> builder.endNullable() -> getAlphaTrianglesLayer().draw(built), and is invoked from renderRect() (741) during the UIColorPicker.render() GUI recording window when editAlpha. This is the dropped-immediate-draw trap: the 1.21.6+ two-phase GUI only composites what is recorded in
  - **Рекомендация:** Replace the Tessellator + RenderLayer.draw with a deferred GuiRenderState submission. The swatch is an opaque-top -> transparent-bottom vertical fade, which the existing batcher.gradientVBox (already used at line 723 for the alpha slider) produces via context.fillGradient; or build a POSITION_COLOR mesh and submit via Batcher2D.drawQuadMesh (GuiQuadMesh.State). Then delete ALPHA_TRIANGLES, the alphaTrianglesLayer field and getAlphaTrianglesLayer().
- **`src/client/java/mchorse/bbs_mod/ui/framework/elements/utils/Batcher2D.java:79-144`** — `Batcher2D.GUI_QUADS / GUI_TRIANGLES / GUI_TRIANGLE_FAN / guiQuadsLayer / guiTrianglesLayer / guiTriangleFanLayer / guiColorBuilder() / layer() / getQuadsLayer() / getTrianglesLayer() / getTriangleFanLayer() / flush(BufferBuilder,RenderLayer)` — _unused-position-color-pipeline-cluster_ · conf=high
  - **Доказательство:** private static final RenderPipeline GUI_QUADS = RenderPipelines.register(guiColorBuilder("gui_color_quads", VertexFormat.DrawMode.QUADS).build()); ...  private static RenderLayer getQuadsLayer() { return guiQuadsLayer = layer(GUI_QUADS, "gui_color_quads", guiQuadsLayer); } ... private static void flush(BufferBuilder builder, RenderLayer renderLayer) { BuiltBuffer built = builder.endNullable(); if (built != null) { renderLayer.draw(built); } }
  - **Почему:** This is the entire old immediate-mode RenderLayer.draw infrastructure (3 registered POSITION_COLOR pipelines, their cached RenderLayers, the builder/layer factory helpers, and the buffer flush helper). After the port moved all 2D drawing onto context.fill / context.fillGradient / drawQuadMesh (deferred GuiRenderState), nothing calls getQuadsLayer()/getTrianglesLayer()/getTriangleFanLayer(). Repo-wide grep confirms zero callers (the only other getTrianglesLayer is a separate copy in UIClipRendere
  - **Вердикт верификатора:** Re-read lines 79-144. The three POSITION_COLOR pipelines (GUI_QUADS/GUI_TRIANGLES/GUI_TRIANGLE_FAN) are registered at class-load but only consumed by getQuadsLayer/getTrianglesLayer/getTriangleFanLayer. Repo-wide grep for getQuadsLayer/getTrianglesLayer/getTriangleFanLayer returns ONLY their definition lines in Batcher2D (the getTrianglesLayer in UIClipRenderer.java:57 is a separate private copy w
  - **Рекомендация:** Delete the three pipeline registrations, the three guiXxxLayer fields, guiColorBuilder(), layer(), the three getXxxLayer() accessors, and static flush(BufferBuilder,RenderLayer), plus the now-orphaned imports (Tessellator, BlendFunction, DepthTestFunction, VertexFormat, VertexFormats, RenderSetup; BufferBuilder/BuiltBuffer once fillTexturedBox is also removed).
- **`src/client/java/mchorse/bbs_mod/ui/framework/elements/utils/Batcher2D.java:634-642`** — `Batcher2D.fillTexturedBox(BufferBuilder,Matrix3x2fc,...)` — _dead-textured-quad-helper_ · conf=high
  - **Доказательство:** private void fillTexturedBox(BufferBuilder builder, Matrix3x2fc matrix, int color, float x, float y, float w, float h, float u1, float v1, float u2, float v2, int textureW, int textureH) {     builder.vertex(matrix, x, y + h).texture(u1 / (float) textureW, v2 / (float) textureH).color(color);     ... (6-vertex two-triangle layout) }
  - **Почему:** Private helper that builds a textured two-triangle quad into a BufferBuilder for the old immediate path. All textured drawing now goes through context.drawTexture (AdoptedTexture bridge) in texturedBox / drawAdoptedGlTexture; this helper has no callers anywhere in the repo. It is residue of the pre-bridge texture path.
  - **Вердикт верификатора:** Re-read lines 634-642: a private 6-vertex two-triangle textured quad builder writing into a BufferBuilder (old immediate path). Repo-wide grep for fillTexturedBox returns only the definition at line 634 - zero callers. All textured drawing now goes through drawAdoptedGlTexture() -> context.drawTexture(GUI_TEXTURED) (lines 613-632). This is pre-bridge residue, accidental dead code safe to delete. I
  - **Рекомендация:** Delete fillTexturedBox; unreachable residue of the removed immediate textured-quad path.
- **`src/client/java/mchorse/bbs_mod/ui/framework/elements/utils/Batcher2D.java:79-93, 100-144`** — `GUI_QUADS/GUI_TRIANGLES/GUI_TRIANGLE_FAN pipelines + gui*Layer fields + getQuadsLayer/getTrianglesLayer/getTriangleFanLayer + flush(BufferBuilder,RenderLayer)` — _unused-immediate-draw-residue_ · conf=high _(added in verify)_
  - **Доказательство:** private static void flush(BufferBuilder builder, RenderLayer renderLayer){ BuiltBuffer built = builder.endNullable(); if (built != null){ renderLayer.draw(built); } } -- repo-wide grep shows getQuadsLayer/getTrianglesLayer/getTriangleFanLayer have ZERO callers, and this flush(BufferBuilder,RenderLayer) at line 136 has ZERO call sites (the only flush() invoked anywhere is the unrelated public no-arg flush() at line 781). The three GUI_* POSITION_COLOR pipelines (79-89) feed only those dead getters.
  - **Рекомендация:** Delete GUI_QUADS/GUI_TRIANGLES/GUI_TRIANGLE_FAN (79-89), the three gui*Layer fields (91-93), getQuadsLayer/getTrianglesLayer/getTriangleFanLayer (120-133), the private flush(BufferBuilder,RenderLayer) helper (136-144), and the now-unused guiColorBuilder/layer helpers (100-118) if nothing else references them. This is leftover immediate-RenderLayer.draw scaffolding from the GUI migration; Batcher2D was fully moved to the deferred path (context.fill/fillGradient/drawText and drawQuadMesh), so this immediate-draw infra is unreachable residue.
- **`src/client/java/mchorse/bbs_mod/graphics/ModelPreviewRenderer.java:107`** — `ModelPreviewRenderer.begin (setShaderLights ENTITY_IN_UI)` — _overridden-state-call_ · conf=medium
  - **Доказательство:** MinecraftClient.getInstance().gameRenderer.getDiffuseLighting().setShaderLights(DiffuseLighting.Type.ENTITY_IN_UI);
  - **Почему:** begin() is called from exactly ONE site (UIModelRenderer.renderModelToTexture line 262), which on the very next relevant line (269) does `RenderSystem.setShaderLights(this.editorLights())` BEFORE any geometry is drawn, unconditionally replacing the ENTITY_IN_UI Lighting UBO this line binds. The caller's own comment (UIModelRenderer 264-269) states: "ModelPreviewRenderer.begin() just bound the vanilla ENTITY_IN_UI inventory preset (lights from below/front), so without this the in-panel model rend
  - **Вердикт верификатора:** Verified at ModelPreviewRenderer.java:107 (binds DiffuseLighting.Type.ENTITY_IN_UI via gameRenderer.getDiffuseLighting().setShaderLights) and at the sole caller UIModelRenderer.renderModelToTexture. begin() is the only entry point and has exactly one caller (UIModelRenderer:262, confirmed by git grep — every other ModelPreviewRenderer reference touches only the static ACTIVE/TEXTURE fields). On th
  - **Рекомендация:** Remove line 107 (and DiffuseLighting/MinecraftClient imports if otherwise unused); the sole caller always overrides the lighting before any draw. Behavior-neutral cleanup.
- **`src/client/java/mchorse/bbs_mod/graphics/ModelPreviewRenderer.java:129-137`** — `ModelPreviewRenderer.getWidth / getHeight` — _unused-getter_ · conf=low
  - **Доказательство:** public int getWidth() { return this.width; }  public int getHeight() { return this.height; }
  - **Почему:** Neither getter has any caller. The only consumer of a ModelPreviewRenderer instance is UIModelRenderer (field `preview`), which uses preview.getColorGlId() but tracks its own dimensions in previewVw/previewVh (UIModelRenderer 296-297) rather than calling these. `git grep` for preview.getWidth/getHeight and .getWidth()/.getHeight() in UIModelRenderer finds no preview-targeted call.
  - **Вердикт верификатора:** Verified getWidth()/getHeight() at lines 129-137 return private width/height. The only ModelPreviewRenderer instance is UIModelRenderer.preview (field at UIModelRenderer:72). Grepping UIModelRenderer shows it consumes preview.getColorGlId() (295) but tracks dimensions in its own previewVw/previewVh fields (296-297), never calling preview.getWidth()/getHeight(). The .getWidth()/.getHeight() hits in
  - **Рекомендация:** Safe to delete both getters as unused accessors. Low priority; could be kept as API surface.
- **`src/client/java/mchorse/bbs_mod/ui/framework/elements/utils/UIModelRenderer.java:354-384 (doc 82-96, 345-353; field decls 93-96)`** — `UIModelRenderer.editorLights / lightDirA / lightDirB / lightsBuffer` — _redundant-per-frame-ubo-upload_ · conf=low
  - **Доказательство:** this.lightDirA.set(LIGHT_A); this.lightDirB.set(LIGHT_B); ... else { RenderSystem.getDevice().createCommandEncoder().writeToBuffer(this.lights, data); }  -- with field/method doc claiming the buffer is 'RE-WRITTEN every frame because the original transformed the two directions by the LIVE camera.view' / 'the directions depend on the per-frame camera view'.
  - **Почему:** The Javadoc on the lights fields (82-96) and on editorLights() (345-353) justifies a per-frame UBO rewrite by a camera.view dependency, but the body (356-361) deliberately binds the directions RAW/constant (LIGHT_A/LIGHT_B), per the corrective inline comment. Since the uploaded bytes are now identical every frame, the else-branch writeToBuffer (379) re-uploads constant data on every world-phase frame -- redundant work, and lightDirA/lightDirB instance fields just hold copies of the two constants
  - **Вердикт верификатора:** Verified the facts: editorLights() sets lightDirA/lightDirB to the constants LIGHT_A/LIGHT_B (360-361, confirmed those are the ONLY writers via grep), then on every frame after the first re-uploads identical bytes via createCommandEncoder().writeToBuffer(this.lights, data) (379). Because the inline comment (356-359) deliberately binds the directions RAW (untransformed), the uploaded data is consta
  - **Рекомендация:** Write the constant directions into lightsBuffer once at allocation and drop the per-frame else-branch writeToBuffer; remove lightDirA/lightDirB and use LIGHT_A/LIGHT_B directly. Fix the Javadoc that wrongly claims a live-camera-view transform.
- **`src/main/resources/assets/bbs/shaders/core/picker_billboard.vsh:9-11 (picker_billboard.vsh); picker_models.vsh 9,11`** — `picker_billboard.vsh in ivec2 UV1/UV2/Normal; picker_models.vsh in ivec2 UV1, in vec3 Normal` — _unused-vertex-attributes_ · conf=low
  - **Доказательство:** picker_billboard.vsh: in ivec2 UV1; in ivec2 UV2; in vec3 Normal;  -- body only uses Position/Color/UV0. picker_models.vsh: in ivec2 UV1; in vec3 Normal; -- body uses UV2 (texCoord2) but never UV1 or Normal.
  - **Почему:** These vertex-input declarations are never read in the shader body. They match the bound POSITION_COLOR_TEXTURE_OVERLAY_LIGHT_NORMAL format so are harmless, but are dead declarations (the picker only needs Position/Color/UV0, plus UV2 for picker_models' sub-index).
  - **Вердикт верификатора:** Read both files. picker_billboard.vsh declares in ivec2 UV1 (9), in ivec2 UV2 (10), in vec3 Normal (11) but main() only uses Position/Color/UV0. picker_models.vsh declares in ivec2 UV1 (9) and in vec3 Normal (11); main() uses UV2 (texCoord2=UV2, line 23) but never UV1 or Normal. These unread in-attributes match the bound POSITION_COLOR_TEXTURE_OVERLAY_LIGHT_NORMAL format so they are harmless docum
  - **Рекомендация:** Optionally drop the unread in-attributes for clarity, or keep them to document the bound vertex format. Cosmetic only.

### ⏭️ Намеренные заглушки незаконченных фич (deferred) — 25

- **`src/client/java/mchorse/bbs_mod/client/BBSRendering.java:445-459`** — `BBSRendering.onRenderChunkLayer(Matrix4f positionMatrix)` — _iris-only-noop-body_ · conf=high
  - **Доказательство:** public static void onRenderChunkLayer(Matrix4f positionMatrix) {     /* TODO(1.21.11 render): this Iris-only chunk-layer hook ... Iris support is      * permanently disabled (isIrisShadersEnabled() == false), so this path is currently dead ... */     if (isIrisShadersEnabled())     {         /* renderCoolStuff(context) — needs a reconstructed WorldRenderContext (see TODO above). */     } }
  - **Почему:** The method is reachable (WorldRendererMixin.java:49 calls it inside the active chromaSky branch), but its entire body is guarded by isIrisShadersEnabled() which hardcodes `return false`, and even the guarded block is empty. The `positionMatrix` parameter is therefore unused. Net effect: a no-op invoked once per chunk-layer render when chromaSky is on (cancelling the vanilla layer at WorldRendererMixin:51 but doing nothing in its place). Documented intentional deferral of the Iris path.
  - **Вердикт верификатора:** Re-read confirms. Body is fully guarded by isIrisShadersEnabled() (line 455) which hardcodes return false (line 537), and the guarded block (lines 456-458) is itself empty. The `positionMatrix` parameter is unused. Reachable from WorldRendererMixin.java:49 inside the active chromaSky branch (lines 47-52: chromaSkyEnabled && !chromaSkyTerrain -> call onRenderChunkLayer then info.cancel()); the seco
  - **Рекомендация:** Leave as-is (flagged TODO). Separately verify the chromaSky-on/chromaSkyTerrain-off branch in WorldRendererMixin:47-52 is meant to drop the vanilla solid terrain layer with no replacement draw — that is outside the three audit categories but worth a behavioral check.
- **`src/client/java/mchorse/bbs_mod/client/renderer/item/GunItemRenderer.java:47-55`** — `GunItemRenderer.render` — _uncalled-render-method_ · conf=high
  - **Доказательство:** public void render(ItemStack stack, ItemDisplayContext mode, MatrixStack matrices, VertexConsumerProvider vertexConsumers, int light, int overlay) { // TODO ... this.get(stack); }
  - **Почему:** Same as ModelBlockItemRenderer: no DynamicItemRenderer interface, registration dropped (BBSModClient 633-636). render(...) has zero callers across the repo; only get(stack) (BBSModClient 285, 707) and update() (522) are reachable. The render body is a no-op get(stack). Class kept alive by get/update; only render(...) is dead. Intentional deferral for the item-model re-port.
  - **Вердикт верификатора:** Verified: render(ItemStack, ItemDisplayContext, MatrixStack, VertexConsumerProvider, int, int) (47-55) is a no-op that only calls this.get(stack). No DynamicItemRenderer interface (class decl 24, header TODO 17-23); registration dropped (BBSModClient 633-636). Caller grep confirms render(...) has ZERO callers; only get(stack) (BBSModClient 285, 707) and update() (BBSModClient 522) are reachable, k
  - **Рекомендация:** Re-implement via the item-model / SpecialModelRenderer system, or delete the dead render(...) method.
- **`src/client/java/mchorse/bbs_mod/client/renderer/item/ModelBlockItemRenderer.java:51-58`** — `ModelBlockItemRenderer.render` — _uncalled-render-method_ · conf=high
  - **Доказательство:** public void render(ItemStack stack, ItemDisplayContext mode, MatrixStack matrices, VertexConsumerProvider vertexConsumers, int light, int overlay) { // TODO ... this.get(stack); }
  - **Почему:** The class no longer implements DynamicItemRenderer (BuiltinItemRendererRegistry/DynamicItemRenderer removed in the 1.21.4 item-model rewrite) and its registration was dropped in BBSModClient (see comment lines 633-636). Grepping all callers, render(...) is never invoked anywhere; only get(stack) (BBSModClient lines 278, 706) and update() (line 521) are still live. The render body is a no-op that just calls get(stack). The class itself is NOT dead (get/update keep the item->ModelProperties cache 
  - **Вердикт верификатора:** Verified: render(ItemStack, ItemDisplayContext, MatrixStack, VertexConsumerProvider, int, int) (51-58) has a no-op body that only calls this.get(stack). The class does not implement DynamicItemRenderer (confirmed at class decl line 28 and the header TODO 21-27), and registration was dropped (BBSModClient 633-636 comment: 'Registrations dropped to keep the build green'). Caller grep confirms render
  - **Рекомендация:** Either re-wire render(...) into the new item-model / SpecialModelRenderer pipeline, or delete the dead render(...) signature; the get/update caching it fronts stands alone.
- **`src/client/java/mchorse/bbs_mod/client/renderer/ModelBlockEntityRenderer.java:44, 51-79`** — `ModelBlockEntityRenderer.renderShadow / static ActorEntity entity` — _neutralized-shadow-stub_ · conf=high
  - **Доказательство:** private static ActorEntity entity; ... public static void renderShadow(...) { ... if (entity == null || entity.getEntityWorld() != world) { entity = new ActorEntity(BBSMod.ACTOR_ENTITY, world); } entity.setPos(x, y, z); entity.lastRenderX = x; ... matrices.push(); matrices.translate(tx, ty, tz); /* ... neutralized ... */ matrices.pop(); }
  - **Почему:** renderShadow is a neutralized stub: it allocates and positions a static ActorEntity (lines 55-67) but the method body only does a balanced push/translate/pop and draws nothing — the EntityRenderDispatcher#renderShadow path and the getSquaredDistanceToCamera distance fade it fed were removed by the 1.21.2 entity render-state rewrite. The whole static ActorEntity field and its per-call position bookkeeping is computed-but-never-used. The lone caller (render, line 211) passes null for the VertexCon
  - **Вердикт верификатора:** Verified: renderShadow (51-79) allocates/refreshes a static ActorEntity (55-58), sets its pos and lastRenderX/Y/Z + lastX/Y/Z (60-66), then does only a balanced push/translate(tx,ty,tz)/pop (68-78) and draws nothing — the body is a neutralized stub (TODO 71-76 confirms EntityRenderDispatcher#renderShadow and the getSquaredDistanceToCamera distance-fade overload were removed by the 1.21.2 rewrite).
  - **Рекомендация:** Keep the empty stub if the shadow feature will be re-ported, but delete the dead static ActorEntity field + its allocation and lastRender*/last* assignments now (re-add when the shadow draw is re-implemented on the OrderedRenderCommandQueue). Note both callers: render() line 211 (null provider) and BaseFilmController line 199 (real provider).
- **`src/client/java/mchorse/bbs_mod/cubic/render/vanilla/ArmorRenderer.java:161-187`** — `ArmorRenderer.renderArmorParts / renderTrim / renderGlint` — _neutralized-draw-stubs_ · conf=high
  - **Доказательство:** private void renderArmorParts(...) { /* TODO(1.21.11 render): the old draw path was ... RenderLayer.getArmorCutoutNoCull was removed ... */ Identifier texture = this.getArmorTexture(assetId, secondTextureLayer, overlay); }  // texture computed then unused; renderTrim similarly computes trimTexture unused; renderGlint body is empty
  - **Почему:** All three private draw helpers are deliberate placeholders: their bodies compute a texture/trim Identifier and then do nothing with it (no VertexConsumer, no part.render, no draw). They are reached from renderArmorSlot (which IS called via ActorEntityRenderer.armorRenderer.renderArmorSlot in ModelFormRenderer.renderArmor), so the public entry runs but produces zero armor pixels. The 1.21.4+ equipment rewrite removed RenderLayer.getArmorCutoutNoCull/getArmorEntityGlint and the per-ModelPart draw 
  - **Вердикт верификатора:** Re-read all three helpers. renderArmorParts (161-171) computes 'Identifier texture = this.getArmorTexture(...)' then does nothing with it (no VertexConsumer, no part.render). renderTrim (173-180) computes 'trimTexture = trim.getTextureId(...)' then nothing. renderGlint (182-187) has an empty body. All three carry explicit TODO(1.21.11 render) comments explaining the 1.21.4+ equipment rewrite remov
  - **Рекомендация:** Leave as intentional deferral: rewire armor/trim/glint through the new EquipmentRenderer + OrderedRenderCommandQueue / AtlasManager path. The computed local Identifiers are currently dead and could be dropped to silence 'unused' warnings, but keeping them documents the intended draw inputs.
- **`src/client/java/mchorse/bbs_mod/cubic/render/vanilla/ArmorRenderer.java:61-71`** — `ArmorRenderer.bakery` — _retained-unused-field_ · conf=high
  - **Доказательство:** private final BakedModelManager bakery; ... this.bakery = bakery; /* TODO(1.21.11 render): BakedModelManager.getAtlas(ARMOR_TRIMS_ATLAS_TEXTURE) was removed ... The bakery is retained for when the trim sprite lookup is rewired ... */
  - **Почему:** The bakery field is assigned in the constructor and never read anywhere else in the class (BakedModelManager.getAtlas(Identifier), the only former consumer, was removed in 1.21.x; atlases now live in AtlasManager). It is a deliberate placeholder kept so the trim-sprite lookup can be re-wired, and the caller (ActorEntityRenderer) still passes MinecraftClient.getInstance().getBakedModelManager().
  - **Вердикт верификатора:** Verified: 'private final BakedModelManager bakery' (line 61) is assigned only in the constructor (line 70) and read nowhere else in the class (its former consumer BakedModelManager.getAtlas(Identifier) was removed in 1.21.x; atlases now live in AtlasManager). The TODO at 67-69 explicitly states the bakery is retained for when the trim-sprite lookup is re-wired through the new equipment-asset pipel
  - **Рекомендация:** Keep while armor rendering is deferred; when the trim path is ported, replace this with the AtlasManager lookup. If the deferral is abandoned, drop the field and the constructor arg.
- **`src/client/java/mchorse/bbs_mod/forms/renderers/BillboardFormRenderer.java:257-262, 103-106`** — `BillboardFormRenderer.fill (POSITION_TEXTURE_LIGHT_COLOR branch)` — _unreachable-format-branch_ · conf=high
  - **Доказательство:** if (format == VertexFormats.POSITION_TEXTURE_LIGHT_COLOR){ return consumer.vertex(matrix, x, y, 0F).texture(u, v).light(light).color(...); }  -- but the only format ever passed is POSITION_COLOR_TEXTURE_OVERLAY_LIGHT_NORMAL (lines 72 and 108); the no-shading path that would use the other format does 'if (!shading){ return; }' at 103-106.
  - **Почему:** The no-shading billboard path is intentionally disabled (no non-picker BBS RenderLayer for it yet), so POSITION_TEXTURE_LIGHT_COLOR is never the format argument. The first branch of fill() is unreachable.
  - **Вердикт верификатора:** Verified: fill() at 259-262 has a POSITION_TEXTURE_LIGHT_COLOR branch. The only format ever constructed/passed is POSITION_COLOR_TEXTURE_OVERLAY_LIGHT_NORMAL (line 72 in renderInUI, line 108 in render3D). The no-shading path that would use the other format early-returns at 103-106 ('if (!shading){ return; }'). So POSITION_TEXTURE_LIGHT_COLOR is never the argument and the first fill() branch is unr
  - **Рекомендация:** Keep tied to restoring the no-shading path: when a non-picker no-shading layer is added and render3D stops early-returning, this branch revives. Otherwise delete with the no-shading code.
- **`src/client/java/mchorse/bbs_mod/forms/renderers/FramebufferFormRenderer.java:128-208`** — `FramebufferFormRenderer.renderModel / renderQuad` — _composite-no-op-discarded-buffer_ · conf=high
  - **Доказательство:** BufferBuilder builder = Tessellator.getInstance().begin(...); /* Front / Back fills ... */ net.minecraft.client.render.BuiltBuffer __bbsBuilt = builder.endNullable(); if (__bbsBuilt != null) { __bbsBuilt.close(); }
  - **Почему:** renderModel computes the quad/uvQuad geometry and renderQuad builds a full vertex buffer, then immediately closes (discards) the BuiltBuffer without drawing it. The whole composite-to-screen path is dead: the inner forms are still rendered into the FBO above (apply/resize/clear/super.renderBodyParts), but that FBO texture is never blitted, so all the upstream FBO work computes a result that is never used. Compositing is stubbed.
  - **Вердикт верификатора:** Confirmed. renderModel computes quad/uvQuad geometry (lines 130-156) and renderQuad fills a full vertex buffer for Front (179-185) and Back (188-194) faces, then at lines 200-205 builds the buffer with `builder.endNullable()` and immediately closes/discards it (`if (__bbsBuilt != null) { __bbsBuilt.close(); }`) without ever drawing it. The TODO at 196-199 states the built quad must be submitted vi
  - **Рекомендация:** Submit __bbsBuilt through a textured RenderLayer/RenderPipeline (Sampler0 bound to framebuffer.getMainTexture()) once the Texture->GpuTextureView bridge lands. Until then the framebuffer form shows nothing in-world.
- **`src/client/java/mchorse/bbs_mod/forms/renderers/LabelFormRenderer.java:133-152`** — `LabelFormRenderer.render3D (picking branch)` — _neutralized-picking-branch_ · conf=high
  - **Доказательство:** if (context.isPicking()) {     /* TODO(1.21.11 render): picking ... Neutralized so the label still renders normally. */     light = 0; } ... CustomVertexConsumerProvider.clearRunnables();
  - **Почему:** The original picking branch installed a picker-shader layer override via CustomVertexConsumerProvider.hijackVertexFormat(...) + RenderSystem.setShader(getPickerModelsProgram). The port reduces the whole branch to light = 0, so during a picking pass the label text is drawn through the normal vanilla text layer (it writes glyph colour, not a Target index) and the label is not pickable. The trailing CustomVertexConsumerProvider.clearRunnables() now clears a runnable this method no longer sets (harm
  - **Вердикт верификатора:** Confirmed. The picking branch (lines 133-141) is reduced to `light = 0;` with a TODO (135-139) stating the picker layer override via CustomVertexConsumerProvider + per-pass Target UBO must be wired 'once the picking foundation lands. Neutralized so the label still renders normally.' During a picking pass the label text therefore draws through the normal vanilla text RenderLayer (writes glyph colou
  - **Рекомендация:** Wire the picker layer override + per-pass Target UBO when the picking foundation lands; the light=0 path is a placeholder. clearRunnables() can stay as defensive cleanup or be removed with the picking rework.
- **`src/client/java/mchorse/bbs_mod/forms/renderers/MobFormRenderer.java:42-43, 58-66`** — `MobFormRenderer.currentPose / currentPoseOverlay (+ getCurrentPose/getCurrentPoseOverlay)` — _always-null-state-by-stub_ · conf=high
  - **Доказательство:** private static Pose currentPose; private static Pose currentPoseOverlay; ... public static Pose getCurrentPose(){ return currentPose; }  -- repo-wide grep for 'currentPose =' / 'currentPoseOverlay =' returns NO matches: the fields are never assigned. The reader LivingEntityRendererMixin.onSetAngles does 'if (pose != null)' (line 29) so its whole pose-application block is dead-by-transitivity while these stay null.
  - **Почему:** These were set by the MobForm vanilla-entity render path, which is stubbed (render3D/renderInUI Stage C). With the writer gone the fields are permanently null; the getters and the mixin block they feed are inert until Stage C is implemented.
  - **Вердикт верификатора:** Verified: fields declared at 42-43, getters at 58-66. Repo-wide grep for 'currentPose =' / 'currentPoseOverlay =' returns NO assignment anywhere, so the fields are permanently null. The only readers are LivingEntityRendererMixin.java:26-27 (getCurrentPose/getCurrentPoseOverlay), whose pose-application block is gated 'if (pose != null)' and is therefore dead-by-transitivity while the fields stay nu
  - **Рекомендация:** Re-assign currentPose/currentPoseOverlay when the MobForm render-state path (Stage C) is ported; the mixin pose hook is already in place and will activate then. Do not delete prematurely.
- **`src/client/java/mchorse/bbs_mod/forms/renderers/MobFormRenderer.java:202-236`** — `MobFormRenderer.renderInUI / render3D` — _stubbed-render-path_ · conf=high
  - **Доказательство:** protected void renderInUI(...){ this.ensureEntity(); /* TODO(1.21.11 render): vanilla-entity rendering is stubbed ... */ }  protected void render3D(...){ this.ensureEntity(); /* TODO ... stubbed ... */ }  -- bodies do nothing but construct/tick the entity.
  - **Почему:** Known intentional Stage C deferral: the 1.21.2 EntityRenderState rewrite (render(EntityRenderState, CameraRenderState, ..., OrderedRenderCommandQueue)) plus removed ShaderProgram/setupTarget picking and the 2D Matrix3x2f GUI matrix are not yet bridged, so the mob form draws nothing. Recorded for completeness as an intentional render no-op.
  - **Вердикт верификатора:** Verified: renderInUI (202-217) and render3D (219-236) bodies only call ensureEntity() followed by a TODO(1.21.11 render) block describing the deferral; they draw nothing. The comments cite the 1.21.2 EntityRenderState rewrite (render(EntityRenderState, CameraRenderState, ..., OrderedRenderCommandQueue)), removed ShaderProgram/setupTarget picking, and the 2D Matrix3x2fStack GUI matrix as the blocke
  - **Рекомендация:** Re-implement against the render-state + command-queue API and the picker pipeline once the entity-render foundation is ported (the documented MobFormRenderer Stage C work).
- **`src/client/java/mchorse/bbs_mod/forms/renderers/ModelFormRenderer.java:609-673`** — `ModelFormRenderer.renderArm` — _renderarm-no-caller_ · conf=high
  - **Доказательство:** @Override public boolean renderArm(MatrixStack matrices, int light, AbstractClientPlayerEntity player, Hand hand){ ... } -- repo-wide grep for renderArm( finds only the definition in FormRenderer, this override, and the internal super.renderArm call; no first-person/HeldItemRenderer mixin exists under src/client/.../mixin to invoke it.
  - **Почему:** In 1.21.1 BBS this override is driven from a first-person-arm mixin (HeldItem/AbstractFirstPersonContext). That mixin was not ported, so renderArm is unreachable. Its body still allocates/poses the model and calls the dead getMainShader (line 655), so it is dead-by-missing-caller.
  - **Вердикт верификатора:** Repo-wide grep for renderArm( returns only: the base definition FormRenderer.java:154, this override ModelFormRenderer.java:609, and the internal super.renderArm call at 672. No first-person/HeldItem mixin under src/client/.../mixin invokes FormRenderer.renderArm, so the override is unreachable. Its body (609-673) still poses the model and calls the dead getMainShader at 655. In 1.21.1 BBS this is
  - **Рекомендация:** Either port the first-person-arm mixin that should call FormUtilsClient.getRenderer(form).renderArm(...), or remove this override until the FP-arm feature is wired. Flag, do not silently delete, since it is a not-yet-ported feature.
- **`src/client/java/mchorse/bbs_mod/mixin/client/BlockEntityRenderDispatcherMixin.java:15-44`** — `BlockEntityRenderDispatcherMixin` — _mixin-not-registered_ · conf=high
  - **Доказательство:** // TODO(1.21.11 render): re-port and re-enable in bbs.client.mixins.json // BlockEntityRenderDispatcher renamed to BlockEntityRenderManager; the render/renderEntity // methods targeted below no longer exist in the state-based pipeline. @Inject method= // strings are inert (mixin not applied at runtime) and kept for re-porting reference. @Mixin(BlockEntityRenderManager.class) public class BlockEntityRenderDispatcherMixin
  - **Почему:** Not listed in bbs.client.mixins.json, so none of the three @Inject hooks (onRenderMain/onRenderToo/onRenderEntity) run. The targeted method signatures (render(BlockEntityRenderer,...)/render(BlockEntity,...)/renderEntity) were removed by the state-based pipeline, so the descriptors are inert. The class is kept only 'for re-porting reference' per its own header comment. No external references exist.
  - **Вердикт верификатора:** Verified: not present in bbs.client.mixins.json (client array lines 5-30), so none of the three @Inject hooks (onRenderMain line 19, onRenderToo line 28, onRenderEntity line 37) run. The targeted method descriptors render(BlockEntityRenderer,BlockEntity,F,MatrixStack,VertexConsumerProvider) and render(BlockEntity,F,MatrixStack,VertexConsumerProvider) reference MatrixStack/VertexConsumerProvider-ba
  - **Рекомендация:** Either re-port the chromaSky terrain-suppression onto the new BlockEntityRenderManager state pipeline and register it in bbs.client.mixins.json, or delete the file. As-is it is dead reference residue with inert @Inject descriptors targeting removed methods.
- **`src/client/java/mchorse/bbs_mod/mixin/client/LivingEntityRendererMixin.java:19-24`** — `LivingEntityRendererMixin.onSetAngles / onRenderEnd` — _mixin-not-registered-superseded_ · conf=high
  - **Доказательство:** // TODO(1.21.11 render): re-add when render path ported @Mixin(LivingEntityRenderer.class) public abstract class LivingEntityRendererMixin {     @Inject(method = "render", at = @At(value = "INVOKE", target = "Lnet/minecraft/client/render/entity/model/EntityModel;setAngles(Lnet/minecraft/entity/Entity;FFFFF)V", ordinal = 0, shift = At.Shift.AFTER))
  - **Почему:** Not listed in bbs.client.mixins.json, so neither inject runs. Its @Inject target 'EntityModel.setAngles(Entity,FFFFF)' and the old immediate render(LivingEntity,float,float,MatrixStack,VertexConsumerProvider,int) signature no longer exist on 1.21.11 (render is now render(state,matrices,queue,camera)). The active MobForm pose path was re-ported into the registered LivingEntityRendererMorphMixin (snapshot+cancel, draw deferred to MorphRenderer). This old file is the superseded predecessor kept for
  - **Вердикт верификатора:** Verified: not in bbs.client.mixins.json, so neither @Inject runs (onSetAngles line 23, onRenderEnd line 95). The registered mixin list (lines 21-23 of the json) includes EntityRendererMixin, LivingEntityRendererMorphMixin and LivingEntityRendererInvoker — i.e. the active MobForm pose path was re-ported into LivingEntityRendererMorphMixin per established memory (snapshot+cancel, draw deferred to Mo
  - **Рекомендация:** Delete the file; its pose-injection responsibility is already covered by the registered LivingEntityRendererMorphMixin + MobFormRenderer path. Keeping it risks a maintainer re-registering a mixin whose @Inject targets (setAngles / old render signature) cannot resolve on 1.21.11.
- **`src/client/java/mchorse/bbs_mod/mixin/client/WorldRendererMixin.java:19-87`** — `WorldRendererMixin (whole file)` — _mixin-not-registered_ · conf=high
  - **Доказательство:** // TODO(1.21.11 render): re-add when render path ported @Mixin(WorldRenderer.class) public class WorldRendererMixin ...     @Inject(method = "setupFrustum", at = @At("HEAD"))     private void onSetupFrustum(Vec3d pos, Matrix4f positionMatrix, Matrix4f projectionMatrix, CallbackInfo info)     {         BBSRendering.camera.set(positionMatrix);     }
  - **Почему:** The file is NOT in bbs.client.mixins.json, so EVERY hook is inert: renderSky/renderLayer chromaSky cancels, loadEntityOutlinePostProcessor & onResized framebuffer resizes, and setupFrustum. Some hooks reference removed APIs (renderLayer signature, getSolid, renderSky fog) and are deliberately neutralized with `if (false)` / TODO. But onSetupFrustum, onLoadEntityOutlineShader, and onResized are plain hooks with no neutralization that simply never fire because the whole mixin is unregistered.
  - **Вердикт верификатора:** Verified: WorldRendererMixin is NOT in bbs.client.mixins.json, so every hook is inert. The file contains a mix: onRenderSky (line 26) and onRenderChunkLayer (line 55) are deliberately neutralized via TODO/`if (false)` (lines 37-38, 58-63) because they reference removed APIs (setShaderFogColor, RenderLayer.getSolid); but onRenderLayer (44), onLoadEntityOutlineShader (66), onResized (72) and onSetup
  - **Рекомендация:** Re-port and register the still-live hooks (setupFrustum camera capture + extra-framebuffer resize on loadEntityOutlinePostProcessor/onResized) onto the 1.21.11 WorldRenderer, or delete the file. Currently the entire mixin is dead and the frustum-capture side effect that populates BBSRendering.camera is silently missing (see matrix finding).
- **`src/client/java/mchorse/bbs_mod/ui/framework/elements/utils/Batcher2D.java:417-427`** — `Batcher2D.dropCircleShadow(...) (both overloads)` — _intentional-stub-no-op_ · conf=high
  - **Доказательство:** public void dropCircleShadow(int x, int y, int radius, int segments, int opaque, int shadow) {     /* TODO(1.21.11 render): circular feathered shadow was a custom TRIANGLE_FAN mesh; no native      * two-phase-GUI primitive. Skipped for the prototype (purely decorative). */ }
  - **Почему:** Both dropCircleShadow overloads are empty no-op bodies. They are still called at runtime (e.g. UIDataUtils.java:37), so the call sites compute and pass arguments that are discarded — the circular feathered shadow is never drawn. This is a deliberate not-yet-ported render feature (marked with the TODO(1.21.11 render) deferral marker), not accidental residue.
  - **Вердикт верификатора:** Re-read lines 417-427: both dropCircleShadow overloads (6-arg and 7-arg) have empty bodies carrying TODO(1.21.11 render) comments explaining the circular feathered shadow was a custom TRIANGLE_FAN mesh with no native two-phase-GUI primitive, skipped as purely decorative. Confirmed still called at runtime: UIDataUtils.java:37 invokes the 7-arg overload, so args are computed and discarded - the shad
  - **Рекомендация:** Leave as-is until the feathered circular shadow is reimplemented via a custom GuiRenderState element / TRIANGLE_FAN mesh on the deferred path.
- **`src/client/java/mchorse/bbs_mod/ui/particles/UIParticleSchemeRenderer.java:36-56, 110`** — `UIParticleSchemeRenderer.getWhiteLightmapTexture / whiteLightmapTexture` — _white-lightmap-built-never-bound_ · conf=high
  - **Доказательство:** getWhiteLightmapTexture(); // (line 110, return value discarded)  ... builds a 16x16 NativeImageBackedTexture ("bbs_particle_white_lightmap") and uploads it, but nothing binds it. The accompanying TODO says: "getWhiteLightmapTexture() still builds that texture, but binding it to the picker-particles layer's sampler must be re-wired in the new pipeline."
  - **Почему:** renderUserModel() invokes getWhiteLightmapTexture() purely for its side effect, then never binds the result to any sampler. The lightmap (Sampler2) is now supplied by the engine because the chosen layer BBSShaders.getParticlesLayer() declares .useLightmap(), and the billboard components write a full-bright light (LightmapTextureManager.MAX_LIGHT_COORDINATE / pack(15,15)). So the texture is allocated and GPU-uploaded every editor-preview session with no consumer — pure dead GL work.
  - **Вердикт верификатора:** Verified at lines 36-56 and 110. getWhiteLightmapTexture() builds a 16x16 NativeImageBackedTexture and calls upload(), but line 110 invokes it purely for side effect and discards the return value; nothing binds whiteLightmapTexture to any sampler. The TODO at lines 104-109 explicitly states the texture is built but 'binding it to the picker-particles layer's sampler must be re-wired in the new pip
  - **Рекомендация:** Delete getWhiteLightmapTexture(), the whiteLightmapTexture field, and the line-110 call; the layer's useLightmap() plus full-bright vertex light already deliver the original full-bright result. Alternatively, if the engine lightmap proves insufficient, actually bind it to Sampler2. As-is it is dead GL allocation each preview session.
- **`src/client/java/mchorse/bbs_mod/ui/particles/UIParticleSchemeRenderer.java:128-185`** — `UIParticleSchemeRenderer.renderPlane / calculate / vector` — _gizmo-buffer-built-then-closed_ · conf=high
  - **Доказательство:** BufferBuilder builder = Tessellator.getInstance().begin(VertexFormat.DrawMode.TRIANGLES, VertexFormats.POSITION_COLOR); ... BuiltBuffer built = builder.endNullable(); if (built != null) { built.close(); }  // closed, never drawn
  - **Почему:** The kill-plane gizmo computes 6 vertices (via calculate()/vector) into a POSITION_COLOR buffer and then immediately closes the BuiltBuffer with no layer.draw(). The TODO confirms: "there is no 3D POSITION_COLOR RenderLayer wired here yet, so the buffer is closed without a GPU submit." The whole geometry build, the private calculate() helper and the reusable vector field exist only to feed this no-op draw — dead by transitivity (renderPlane is the only caller of calculate()).
  - **Вердикт верификатора:** Verified at lines 128-161. renderPlane begins a TRIANGLES POSITION_COLOR BufferBuilder, computes 6 vertices via calculate()/vector, then at lines 155-160 calls builder.endNullable() and, if non-null, built.close() with NO layer.draw() call. The geometry is therefore never submitted to the GPU. The TODO at lines 130-135 confirms: 'there is no 3D POSITION_COLOR RenderLayer wired here yet, so the buf
  - **Рекомендация:** Keep as the deferred kill-plane gizmo, but note that calculate()/vector and the entire renderPlane vertex build are wasted CPU work each preview frame the kill plane is active. When a 3D POSITION_COLOR RenderLayer is wired, submit the BuiltBuffer instead of closing it.
- **`src/client/java/mchorse/bbs_mod/utils/PlayerUtils.java:53-86`** — `PlayerUtils.ProtectedAccess` — _unused-inner-class_ · conf=high
  - **Доказательство:** public static class ProtectedAccess extends PlayerEntity {     public static TrackedData<Byte> getModelParts() { ... return null; }     public ProtectedAccess(World world, GameProfile gameProfile) { super(world, gameProfile); }     @Override public net.minecraft.world.GameMode getGameMode() { return ...SURVIVAL; }     @Override public boolean isSpectator() { return false; }     @Override public boolean isCreative() { return false; } }
  - **Почему:** Grep across the whole repo finds the identifier 'ProtectedAccess' only in this file (no 'new ProtectedAccess', no type reference anywhere else). The class is a protected-access bridge whose sole render-relevant purpose was exposing PlayerEntity.PLAYER_MODEL_PARTS via getModelParts(); that tracked-data was removed in 1.21.11 (verified: no PLAYER_MODEL_PARTS / TrackedData<Byte> in the mapped PlayerEntity.java) so getModelParts() now returns null and has no callers, leaving the entire enclosing cla
  - **Вердикт верификатора:** Verified by grep: the identifier 'ProtectedAccess' appears ONLY in this file (declaration at line 53 + constructor at line 64) — no 'new ProtectedAccess', no type reference, no import anywhere else in the repo. Likewise 'getModelParts' appears only at its declaration (line 55). The class is genuinely uninstantiated/unreferenced dead code, so category 'dead' / subtype 'unused-inner-class' is correc
  - **Рекомендация:** Leave in place if the player-model/skin-config path is being finished soon (it is a deliberate TODO(1.21.11 render) placeholder). Otherwise delete the entire ProtectedAccess inner class — it is currently unreachable and removing it is safe (no callers).
- **`src/main/resources/assets/bbs/shaders/core/multilink.fsh:1-46 (vsh+fsh); BBSShaders.java 66,232-240,396-415; UIMultiLinkEditor.java 234-238`** — `MULTILINK pipeline / multilink.vsh+fsh / MultilinkInfo UBO / getMultilinkProgram / getMultilinkLayer` — _stubbed-gui-shader-multilink_ · conf=high
  - **Доказательство:** UIMultiLinkEditor: final RenderPipeline finalProgram = needsMultLinkShader ? BBSShaders.getMultilinkProgram() : RenderPipelines.GUI_TEXTURED; context.batcher.texturedBox(() -> finalProgram, texture.id, ...). Batcher2D.texturedBox(Supplier<RenderPipeline> shader,...) @Deprecated { /* The Supplier (pipeline selector) is ignored. */ this.drawAdoptedGlTexture(...); }
  - **Почему:** The multilink pixelate/erase shader (multilink.vsh/fsh + MultilinkInfo UBO + getMultilinkProgram/getMultilinkLayer + the registered MULTILINK pipeline) is fetched but never actually dispatched: its only caller passes it to the deprecated Batcher2D.texturedBox(Supplier,...) bridge which ignores the pipeline and draws through GUI_TEXTURED. The MultilinkInfo (Filters/Size) UBO is never uploaded, so the shader is dead-by-stub.
  - **Вердикт верификатора:** Verified at UIMultiLinkEditor:234-238: finalProgram = needsMultLinkShader ? getMultilinkProgram() : GUI_TEXTURED, passed to context.batcher.texturedBox(() -> finalProgram, ...). Batcher2D.texturedBox(Supplier,...) is @Deprecated (line 601) and its body explicitly ignores the supplier (comment 'The Supplier (pipeline selector) is ignored') drawing through drawAdoptedGlTexture -> GUI_TEXTURED. So th
  - **Рекомендация:** Keep as deferred work; revive via a manual two-phase-GUI RenderPass binding MultilinkInfo + Sampler0/Sampler3 into an off-screen target (per TODO at UIMultiLinkEditor:222-233). Do not delete.
- **`src/main/resources/assets/bbs/shaders/core/picker_billboard_no_shading.fsh:1-33 (vsh+fsh); BBSShaders.java 112-114,153,201-204,272-280`** — `PICKER_BILLBOARD_NO_SHADING pipeline / picker_billboard_no_shading.vsh+fsh / getPickerBillboardNoShadingProgram / getPickerBillboardNoShadingLayer` — _unused-picker-no-shading_ · conf=high
  - **Доказательство:** grep 'NoShading' finds only comments: BillboardFormRenderer:97 'used picker_billboard_no_shading ONLY for picking ... the no-shading normal path is skipped until a proper non-picker layer is added'; ExtrudedFormRenderer:86. No live call to getPickerBillboardNoShadingProgram() or getPickerBillboardNoShadingLayer() anywhere.
  - **Почему:** The no-shading billboard picking path is not wired in this port (BillboardFormRenderer.render3D returns early when !shading). Its pipeline, both shader files, and both accessors have zero live callers.
  - **Вердикт верификатора:** Grep confirms getPickerBillboardNoShadingProgram (BBSShaders:201) and getPickerBillboardNoShadingLayer (BBSShaders:272) have zero callers beyond their own definitions; only comment mentions of 'NoShading' exist (BillboardFormRenderer:97, ExtrudedFormRenderer:86) describing that the no-shading billboard picking path is not yet wired. So the PICKER_BILLBOARD_NO_SHADING pipeline + both shader files +
  - **Рекомендация:** Keep as deferred picking residue (must eventually go through BBSPickerRenderer.draw like picker_models, since the layer wrapper can't carry the BBSPicker UBO), or delete with the rest of the no-shading billboard path if that form is dropped.
- **`src/main/resources/assets/bbs/shaders/core/subtitles.fsh:1-89 (vsh+fsh); BBSShaders.java 75,429-447; UISubtitleRenderer.java 52-53,193`** — `SUBTITLES pipeline / subtitles.vsh+fsh / SubtitlesInfo UBO / getSubtitlesProgram / getSubtitlesLayer` — _stubbed-gui-shader-subtitles_ · conf=high
  - **Доказательство:** UISubtitleRenderer: RenderPipeline program = BBSShaders.getSubtitlesProgram(); Supplier<RenderPipeline> supplier = () -> program; ... batcher.texturedBox(supplier, texture.id, ...). Comment lines 183-184: 'texturedBox(Supplier,...) routes through the AdoptedTexture -> GUI_TEXTURED bridge (the Supplier/pipeline is IGNORED), so the blur is NOT applied and SubtitlesInfo is never uploaded.'
  - **Почему:** The subtitle blur shader (subtitles.vsh/fsh + SubtitlesInfo UBO + getSubtitlesProgram/getSubtitlesLayer + the registered SUBTITLES pipeline) is fetched into a Supplier but the GUI_TEXTURED bridge ignores it, so the blur path and its SubtitlesInfo (Blur/TextureSize) UBO are never executed. Dead-by-stub.
  - **Вердикт верификатора:** UISubtitleRenderer:52 fetches getSubtitlesProgram() into a Supplier and passes it to batcher.texturedBox(supplier,...). As verified above, that overload is @Deprecated and ignores the pipeline (drawing via GUI_TEXTURED), so the blur path + SubtitlesInfo (Blur/TextureSize) UBO never execute. The on-site comment at UISubtitleRenderer:181 and registerSubtitles:424-427 mark this as deferred 'revive GU
  - **Рекомендация:** Keep as deferred; revive via an off-screen RenderPass binding SubtitlesInfo + Sampler0 over the text FBO (per TODO at UISubtitleRenderer:179-189). Do not delete.
- **`src/client/java/mchorse/bbs_mod/client/BBSShaders.java:262-270, 282-290`** — `BBSShaders.getPickerBillboardLayer / getPickerParticlesLayer (used in ParticleFormRenderer.java 190-192)` — _picker-layer-cannot-carry-ubo_ · conf=medium
  - **Доказательство:** ParticleFormRenderer (picking branch): layer = shadersEnabled ? BBSShaders.getPickerBillboardLayer() : BBSShaders.getPickerParticlesLayer(); ... emitter.render(format, layer, ...). ParticleEmitter.render -> layer.draw(built). The picker_billboard/picker_particles pipelines declare withUniform(PICKER_UNIFORM, UNIFORM_BUFFER) but RenderLayer.draw binds only engine builtins + pipeline samplers (cannot carry the custom BBSPicker UBO).
  - **Почему:** These two picker layers are the only picker layer wrappers with live callers, but they are dispatched through the immediate RenderLayer.draw path, which cannot upload the custom BBSPicker (Target) UBO. The picker_billboard/picker_particles shaders therefore always read a stale/zero Target -- particle picking is non-functional by construction (the TODO at ParticleFormRenderer:183-185 acknowledges 'picking is a no-op until then').
  - **Вердикт верификатора:** Verified ParticleFormRenderer:188-202: in the context.isPicking() branch layer = shadersEnabled ? getPickerBillboardLayer() : getPickerParticlesLayer(), then emitter.render(format, layer, ...). ParticleEmitter.render -> layer.draw(built) (the immediate RenderLayer.draw path). registerPicker (456-473) declares .withUniform(PICKER_UNIFORM, UNIFORM_BUFFER), and the class javadoc (84) states the Rende
  - **Рекомендация:** Route particle picking through BBSPickerRenderer.draw(getPickerBillboardProgram()/getPickerParticlesProgram(), built, modelView) (the manual pass that binds the BBSPicker UBO), mirroring the picker_models path; then remove the two broken layer accessors and the unused *Program accessors become live.
- **`src/client/java/mchorse/bbs_mod/cubic/render/vao/BOBJModelVAO.java:50-51`** — `BOBJModelVAO.delete()` — _noop-lifecycle-method_ · conf=medium
  - **Доказательство:** public void delete() {}
  - **Почему:** Empty no-op that previously freed the raw-GL skinned-mesh VAO/VBOs (now CPU-side). Still called via BOBJModel.java:58 ('this.vao.delete();') during model reload, so reachable but does nothing. Documented lifecycle stub.
  - **Вердикт верификатора:** Verified. delete() is empty (lines 50-51) with a Javadoc (lines 46-49) noting it previously freed the raw-GL VAO/VBOs, now unnecessary since the skinned mesh draws through the immediate BufferBuilder path in render(). Reachable: called from BOBJModel.java:58 ('this.vao.delete();') inside the live BOBJModel.delete(). Documented no-op lifecycle stub; intentional=true is correct.
  - **Рекомендация:** Optional cleanup: keep as documented no-op or remove with its caller. Low priority.
- **`src/client/java/mchorse/bbs_mod/cubic/render/vao/ModelVAO.java:30-31`** — `ModelVAO.delete()` — _noop-lifecycle-method_ · conf=medium
  - **Доказательство:** public void delete() {}
  - **Почему:** Empty no-op. It used to free the raw-GL VAO/VBO handles, which no longer exist (geometry is CPU-side now). It IS still called via the model lifecycle (ModelInstance.java:295 'value.delete();'), so it is not unreachable, but it does nothing. Kept as a lifecycle hook contract; the comment documents this. Reported for awareness only.
  - **Вердикт верификатора:** Verified. delete() is an empty body (lines 30-31) whose Javadoc (lines 27-29) states it previously freed the raw-GL VAOs which no longer exist (geometry is CPU-side). It is reachable, not unreachable: called from ModelInstance.java:295 ('value.delete();') inside ModelInstance.delete(), which is itself a live, non-stubbed lifecycle method. So it is a documented no-op lifecycle hook, correctly inten
  - **Рекомендация:** Optional: keep as a documented lifecycle no-op (harmless, paired with live callers) or remove the method plus its call site if no future GPU resource is expected. Low priority.

## Проблемы с матрицами (matrix) — 10

### ⚙️ Можно удалять/чинить сейчас (actionable) — 1

- **`src/client/java/mchorse/bbs_mod/ui/film/clips/renderer/UIClipRenderer.java:129-153 (helper); allocation at 45-65`** — `UIClipRenderer.renderEnvelope / getTrianglesLayer / GUI_TRIANGLES` — _immediate-draw-dropped-in-two-phase-gui_ · conf=high
  - **Доказательство:** BufferBuilder builder = Tessellator.getInstance().begin(VertexFormat.DrawMode.TRIANGLES, VertexFormats.POSITION_COLOR); ... BuiltBuffer built = builder.endNullable(); if (built != null) { getTrianglesLayer().draw(built); }  -- called from UIClips.renderClip (UIClips.java:1813) during GUI render.
  - **Почему:** This is the exact 1.21.6+ two-phase-GUI trap. renderClip runs inside Screen.render where vanilla only RECORDS draws into the GuiRenderState and composites them afterwards. A self-issued mid-frame immediate RenderLayer.draw(BuiltBuffer) is overpainted by the deferred composite AND runs with the wrong (world) projection, so the clip-envelope triangles never appear in the dope sheet. This is precisely the bug GuiQuadMesh was created to fix: its own class doc (graphics/GuiQuadMesh.java:14-22) says k
  - **Вердикт верификатора:** Verified directly. renderEnvelope (lines 129-153) builds geometry with Tessellator.getInstance().begin(TRIANGLES, POSITION_COLOR), finishes via builder.endNullable(), and submits with getTrianglesLayer().draw(built) — a self-issued immediate RenderLayer.draw(BuiltBuffer). renderClip is invoked at UIClips.java:1813 inside a 2D batcher GUI render loop (uses context.batcher.box/outline/icon/textShado
  - **Рекомендация:** Re-route renderEnvelope through Batcher2D.drawQuadMesh(GuiQuadMesh): emit the envelope as 4-vertex QUADS (each triangle pair becomes a quad), record via vertex(Matrix3x2fc,x,y).color(argb), and submit through the deferred GUI. Then delete the now-unused GUI_TRIANGLES pipeline, guiTrianglesLayer field, getTrianglesLayer(), and the Tessellator/BufferBuilder/BuiltBuffer/RenderLayer/RenderSetup imports.

### ⏭️ Намеренные заглушки незаконченных фич (deferred) — 9

- **`src/client/java/mchorse/bbs_mod/client/renderer/entity/ActorEntityRenderer.java:36-37, 62-73, 80`** — `ActorEntityRenderer.renderedEntity / updateRenderState / render` — _live-entity-on-renderer-field_ · conf=high
  - **Доказательство:** private ActorEntity renderedEntity; ... public void updateRenderState(...) { ... this.renderedEntity = entity; this.renderedTickDelta = tickDelta; ... } ... public void render(...) { ... ActorEntity entity = this.renderedEntity;
  - **Почему:** The live entity + tickDelta are stashed on a SINGLE renderer-instance field, but in the 1.21.2+ render-state model one EntityRenderer instance is shared across every ActorEntity and updateRenderState is called per-entity into pooled render states. render() then reads this.renderedEntity, which holds whatever entity was updated last. This is exactly the per-entity recovery hazard the project's own IRenderStateEntityHolder bridge (set in getAndUpdateRenderState, read off the state) was built to av
  - **Вердикт верификатора:** Verified at the cited lines: `private ActorEntity renderedEntity; private float renderedTickDelta;` (36-37) are instance fields, assigned in updateRenderState (66-67) and read back in render (80) then fed to FormUtilsClient.render (98). EntityRendererRegistry.register(BBSMod.ACTOR_ENTITY, ActorEntityRenderer::new) (BBSModClient:625) creates ONE shared renderer instance per entity type, while updat
  - **Рекомендация:** Drop the renderedEntity/renderedTickDelta renderer fields; recover the live entity and tickDelta per-state via ((IRenderStateEntityHolder) state).bbs$getRenderedEntity()/bbs$getRenderedTickDelta(), the bridge already populated for every EntityRenderState by EntityRendererMixin/EntityRenderStateMixin.
- **`src/client/java/mchorse/bbs_mod/forms/renderers/AnchorFormRenderer.java:44-64`** — `AnchorFormRenderer.renderInUI` — _fresh-stack-invisible_ · conf=high
  - **Доказательство:** MatrixStack stack = new MatrixStack(); Matrix4f uiMatrix = ModelFormRenderer.getUIMatrix(context, x1, y1, x2, y2); ... this.renderBodyParts(new FormRenderingContext().set(FormRenderType.ENTITY, this.entity, stack, ...).inUI());
  - **Почему:** When an AnchorForm has child parts, the body parts are rendered onto a freshly allocated local MatrixStack instead of the live DrawContext, and the result is never composited back into the 2D GUI. The original rendered onto context.batcher.getContext().getMatrices() (a real 3D stack in 1.21.1). This is the documented fresh-MatrixStack-renders-invisible trap compounded by the two-phase GUI dropping the immediate 3D child draws issued during the record phase: the anchor's child-form preview will n
  - **Вердикт верификатора:** Confirmed by reading the file. When the AnchorForm has child parts (else branch), a brand-new MatrixStack is allocated (line 47 `MatrixStack stack = new MatrixStack();`) and child parts are rendered into it via `this.renderBodyParts(new FormRenderingContext().set(FormRenderType.ENTITY, this.entity, stack, ...).inUI())` (lines 60-62). Nothing composites that 3D stack back into the live 2D DrawConte
  - **Рекомендация:** Route through the special-element off-screen FBO preview path (like ModelFormRenderer's list/in-panel preview) so child parts render in the GUI prepare phase and composite correctly. Until then this branch is a no-op for forms with child parts.
- **`src/client/java/mchorse/bbs_mod/ui/forms/editors/utils/UIPickableFormRenderer.java:267-283`** — `UIPickableFormRenderer.renderFormHitbox` — _fresh-matrixstack-no-origin_ · conf=high
  - **Доказательство:** /* TODO(1.21.11 render): getMatrices() is now a 2D Matrix3x2fStack; hitbox boxes render  * in 3D world space, so draw against a fresh MatrixStack at the model origin. */ MatrixStack stack = new MatrixStack(); /* Draw look vector */ final float thickness = 0.01F; Draw.renderBox(stack, -thickness, -thickness + eyeHeight, -thickness, thickness, thickness, 2F, 1F, 0F, 0F); /* Draw hitbox */ Draw.renderBox(stack, -hitboxW / 2, 0, -hitboxW / 2, hitboxW, hitboxH, hitboxW);
  - **Почему:** The form geometry is drawn with createCameraStack() (camera baked into the per-vertex MatrixStack; ModelPreviewRenderer.begin keeps the global model-view identity and supplies only perspective). Draw.renderBox bakes ONLY the passed stack into the vertices and submits through Draw.getPositionColorLayer() (which applies the identity global model-view + projection). Here the stack is a bare `new MatrixStack()` with neither the camera (createCameraStack) NOR even the editor origin (stripScale(getOri
  - **Вердикт верификатора:** Confirmed matrix trap. The form geometry is drawn via createCameraStack() (UIModelRenderer line 334-342: camera.view * translate(-camera.position) * transform baked into the per-vertex MatrixStack), with the global model-view kept identity (perspective only set in ModelPreviewRenderer.begin). Draw.renderBox bakes ONLY the passed stack into vertices (fillBox -> stack.peek().getPositionMatrix()) and
  - **Рекомендация:** Finish per the TODO: apply stripScale(this.formEditor.getOrigin(transition)) like renderAxes does before the Draw.renderBox calls (and match whatever view transform the form geometry uses) so the hitbox visualization aligns with the rendered form.
- **`src/client/java/mchorse/bbs_mod/ui/model_blocks/UIModelBlockPanel.java:82, 252-255, 360-361, 683-693 (consumed in GizmoInteraction.java:166-176)`** — `UIModelBlockPanel.gizmoProjection / getGizmoProjection / renderGizmo / renderInWorld` — _identity-projection-placeholder_ · conf=high
  - **Доказательство:** private final Matrix4f gizmoProjection = new Matrix4f();  ... this.gizmoCamera.projection.set(this.gizmoProjection);  ... this.mouseDirection.set(CameraUtils.getMouseDirection(this.gizmoProjection, new Matrix4f(), ...));  with TODO: 'RenderSystem.getProjectionMatrix() and WorldRenderContext.positionMatrix() are removed ... the gizmo projection/view stay at their previous values, so picking/dragging won't be frame-accurate yet.'
  - **Почему:** gizmoProjection is constructed as an IDENTITY Matrix4f and is never written with a real perspective projection anywhere (no assignment exists; getGizmoProjection() only reads it). It is then fed as the projection to gizmoCamera.projection, to CameraUtils.getMouseDirection (paired with an identity view, new Matrix4f()), and to GizmoInteraction.updateSphereHover (computeScreenCenter/computeScreenRadius). With an identity projection+view the ray-pick and sphere-hover screen math cannot be frame-acc
  - **Вердикт верификатора:** Verified: gizmoProjection is declared 'private final Matrix4f gizmoProjection = new Matrix4f()' (82) = identity. Grep for gizmoProjection in the file returns only declaration (82) and three READS — getGizmoProjection() return (254), gizmoCamera.projection.set(gizmoProjection) (360), and CameraUtils.getMouseDirection(gizmoProjection, new Matrix4f(), ...) (689). There is NO assignment that writes a 
  - **Рекомендация:** When the 1.21.11 render-pipeline foundation exposes the live projection + camera-view, populate gizmoProjection and the view passed to getMouseDirection from it each frame so picking/dragging/sphere-hover become frame-accurate. Until then keep the TODO; do not delete.
- **`src/client/java/mchorse/bbs_mod/client/renderer/entity/ActorEntityRenderer.java:75-102`** — `ActorEntityRenderer.render` — _immediate-draw-in-build-phase-render_ · conf=medium
  - **Доказательство:** public void render(LivingEntityRenderState state, MatrixStack matrices, OrderedRenderCommandQueue queue, CameraRenderState cameraState) { ... this.setupTransforms(state, matrices); ... FormUtilsClient.render(form, new FormRenderingContext().set(FormRenderType.ENTITY, entity.getFormEntity(), matrices, state.light, overlay, this.renderedTickDelta).camera(...));
  - **Почему:** EntityRenderer.render() is now a BUILD phase that only enqueues into the OrderedRenderCommandQueue; the GL draw happens later at queue flush. The project's own LivingEntityRendererMorphMixin documents that the BBS immediate form pipeline 'cannot draw correctly here' and instead snapshots the pose and defers the draw to WorldRenderEvents.AFTER_ENTITIES (MorphRenderer.renderQueued), the only world context where the camera model-view is active. ActorEntityRenderer.render still issues the immediate 
  - **Вердикт верификатора:** Verified: render(LivingEntityRenderState, MatrixStack, OrderedRenderCommandQueue, CameraRenderState) (76) pushes the build-phase matrices, runs setupTransforms, then issues an IMMEDIATE FormUtilsClient.render(...) (97-99) on those matrices rather than enqueuing into the OrderedRenderCommandQueue. The project's own LivingEntityRendererMorphMixin header documents this exact hazard (lines 27-29: rend
  - **Рекомендация:** Route the actor form draw through the same deferred AFTER_ENTITIES path the morph forms use (snapshot pose into a queue, flush in MorphRenderer-style with a camera-relative matrix from getMatrixForRenderWithRotation), or runtime-confirm that the build-phase immediate draw lands.
- **`src/client/java/mchorse/bbs_mod/client/renderer/entity/GunProjectileEntityRenderer.java:49-76`** — `GunProjectileEntityRenderer.render` — _immediate-draw-in-build-phase-render_ · conf=medium
  - **Доказательство:** public void render(GunProjectileRenderState state, MatrixStack matrices, OrderedRenderCommandQueue queue, CameraRenderState cameraState) { ... FormUtilsClient.render(projectile.getForm(), new FormRenderingContext().set(FormRenderType.ENTITY, projectile.getFormEntity(), matrices, state.light, OverlayTexture.DEFAULT_UV, state.tickDelta).camera(...));
  - **Почему:** Same build-phase immediate-draw concern as ActorEntityRenderer: render() only enqueues commands in 1.21.2+, yet this issues an immediate FormUtilsClient.render on the build-phase matrices. (Unlike ActorEntityRenderer, the live entity recovery here is correct — it is carried on the per-entity GunProjectileRenderState.entity, not a shared renderer field — so this finding is purely about the deferred-draw requirement.) TODO(1.21.11 render) at line 70 acknowledges only the depth-state migration, not
  - **Вердикт верификатора:** Verified: render(GunProjectileRenderState, MatrixStack, OrderedRenderCommandQueue, CameraRenderState) (50) issues an immediate FormUtilsClient.render(...) (71-73) on the build-phase MatrixStack, same build-phase-vs-flush concern as ActorEntityRenderer and contradicted by the morph mixin's documented 'cannot draw correctly here' note. The finder's caveat is correct and verified: unlike ActorEntityR
  - **Рекомендация:** If the gun projectile form fails to render, move the draw to the deferred AFTER_ENTITIES path (MorphRenderer/BaseFilmController style) using a camera-relative matrix instead of the build-phase MatrixStack.
- **`src/client/java/mchorse/bbs_mod/mixin/client/WorldRendererMixin.java:83-87`** — `WorldRendererMixin.onSetupFrustum -> BBSRendering.camera` — _missing-camera-transform_ · conf=medium
  - **Доказательство:** @Inject(method = "setupFrustum", at = @At("HEAD")) private void onSetupFrustum(Vec3d pos, Matrix4f positionMatrix, Matrix4f projectionMatrix, CallbackInfo info) {     BBSRendering.camera.set(positionMatrix); }
  - **Почему:** This is the SOLE writer of BBSRendering.camera (declared `public static final Matrix4f camera = new Matrix4f();` in BBSRendering.java:69 — constructed as identity). Because the mixin is unregistered (see dead finding above) this .set() never runs, so BBSRendering.camera stays the identity matrix at runtime. UIFilmController reads it as the view matrix in the picking/stencil path: `InverseView.set(new Matrix3f(BBSRendering.camera).invert())` (UIFilmController.java:1231) and `MatrixStackUtils.mult
  - **Вердикт верификатора:** Verified: BBSRendering.camera is declared `public static final Matrix4f camera = new Matrix4f();` at BBSRendering.java:69 (JOML default-constructs to identity). Grep confirms the ONLY writer is WorldRendererMixin.onSetupFrustum line 86 (BBSRendering.camera.set(positionMatrix)); BBSRendering.renderCoolStuff (lines 502-523) writes InverseView from gameRenderer.getCamera().getRotation() but never tou
  - **Рекомендация:** When re-enabling frustum capture, ensure BBSRendering.camera is populated before UIFilmController's stencil/picking path (lines 1231/1238) consumes it; until then this read silently uses an identity view matrix. Tie the fix to the picking re-port (StencilFormFramebuffer / GpuTextureView bridge) and the WorldRendererMixin re-registration.
- **`src/client/java/mchorse/bbs_mod/ui/particles/UIParticleSchemeRenderer.java:94-118, 187-203`** — `UIParticleSchemeRenderer.renderUserModel / renderGrid` — _fresh-3d-matrixstack-for-gui-preview_ · conf=medium
  - **Доказательство:** MatrixStack stack = new MatrixStack(); stack.push(); stack.loadIdentity(); stack.multiplyPositionMatrix(new Matrix4f(InverseView.get()).invert()); ... this.emitter.render(VertexFormats.POSITION_TEXTURE_COLOR_LIGHT, BBSShaders.getParticlesLayer(), stack, ...);
  - **Почему:** DrawContext.getMatrices() is now a 2D Matrix3x2f and can no longer carry the 3D view, so the preview builds a fresh 3D MatrixStack seeded only with inv(InverseView). This is the classic 1.21.11 2D-vs-3D matrix migration trap. It happens to work here (unlike the in-world fresh-stack-renders-invisible trap) because renderUserModel runs inside UIModelRenderer.renderModelToTexture's FBO render pass (world phase, outside the two-phase-GUI window) so the immediate getParticlesLayer().draw survives, an
  - **Вердикт верификатора:** Verified at lines 98-118 (renderUserModel) and 198-201 (renderGrid). Both construct a fresh new MatrixStack() seeded only with new Matrix4f(InverseView.get()).invert(), because DrawContext.getMatrices() is now a 2D Matrix3x2fStack and can no longer carry the 3D view (TODOs at 94-97 and 194-197 say exactly this). This is the genuine 1.21.11 2D-vs-3D matrix migration pattern, correctly in scope for 
  - **Рекомендация:** When the UIModelRenderer 3D foundation exposes the viewport's model-view stack directly, replace the hand-seeded fresh MatrixStacks in both renderUserModel and renderGrid with the shared viewport stack so camera/view wiring is single-sourced. Verify at runtime the preview is neither blank nor misplaced.
- **`src/client/java/mchorse/bbs_mod/utils/MatrixStackUtils.java:36-41`** — `MatrixStackUtils.cacheMatrices` — _vestigial-global-modelview-mutation_ · conf=medium
  - **Доказательство:** RenderSystem.backupProjectionMatrix(); Matrix4fStack renderStack = RenderSystem.getModelViewStack(); renderStack.pushMatrix(); renderStack.identity();
  - **Почему:** This pushes+identities the GLOBAL RenderSystem model-view stack, a 1.21.1-era pattern. The file's own TODO comment (lines 30-34) notes RenderSystem.applyModelViewMatrix() was removed and 'the model-view stack is auto-applied'. But the only caller, UIFilmController.renderPickingPreview (line 1227), then renders the stencil through worldRenderContext.matrices() (a MatrixStack at line 1234), NOT through the global RenderSystem model-view stack. On 1.21.11 draws routed via RenderLayer/MatrixStack do
  - **Вердикт верификатора:** Verified. cacheMatrices() (lines 37-40) does RenderSystem.getModelViewStack().pushMatrix()/identity() on the GLOBAL model-view stack, balanced by popMatrix() in restoreMatrices() (line 49-51) — push/pop are correctly paired and harmless. The sole caller is UIFilmController.renderPickingPreview (line 1227, restore at 1243). Crucially, the actual stencil render between them does NOT use the global m
  - **Рекомендация:** Leave as-is until the picking path is rebuilt (known-blocked feature). When picking is reworked, drop the getModelViewStack().pushMatrix()/identity() in cacheMatrices() and the matching popMatrix() in restoreMatrices(), keeping only the projection backup/restore pair — the stencil render already supplies its own transform via worldStack.

## Устаревшие вызовы render/GL API (deprecated-api) — 17

### ⚙️ Можно удалять/чинить сейчас (actionable) — 4

- **`src/client/java/mchorse/bbs_mod/client/BBSShaders.java:222-230, 350-352, 505-517`** — `BBSShaders.getModelLayer / BBSShaders.layer` — _sampler0-not-bound-blur_ · conf=high
  - **Доказательство:** registerModel(): .withSampler("Sampler0").withSampler("Sampler1").withSampler("Sampler2"). getModelLayer(): modelLayer = layer("model", MODEL, true). layer(): RenderSetup.Builder setup = ...translucent(); if (useLightmapOverlay) { setup.useLightmap().useOverlay(); } -- only Sampler2 (lightmap) and Sampler1 (overlay) are attached; Sampler0 is never given a sampler state. model.fsh: vec4 color = texture(Sampler0, texCoord0);
  - **Почему:** The MODEL pipeline declares and model.fsh samples Sampler0 (the model albedo), but getModelLayer()/layer() only attaches Sampler1/Sampler2 via useOverlay()/useLightmap(); no NEAREST sampler object is bound for texture unit 0. On 1.21.5+ a leftover GL sampler object on unit 0 (typically LINEAR, from a prior vanilla draw) OVERRIDES the texture's own glTexParameteri(NEAREST), so model/billboard/BOBJ/extruded world textures render blurry. The per-texture NEAREST that worked in 1.21.1 no longer takes
  - **Вердикт верификатора:** Verified directly. registerModel() (line 350-352) declares .withSampler("Sampler0") and model.fsh:6,19 reads `uniform sampler2D Sampler0; ... texture(Sampler0, texCoord0)`. But layer() (505-517) only calls setup.useLightmap().useOverlay() for useLightmapOverlay=true, which attaches SamplerState for Sampler2 (lightmap) and Sampler1 (overlay); no sampler-object state is configured for texture unit 0
  - **Рекомендация:** Bind Sampler0 on the model RenderSetup/RenderLayer with an explicit NEAREST sampler object (same approach as the picker manual passes), or route the albedo through a sampler-state-aware path so the leftover LINEAR sampler object cannot bleed onto unit 0.
- **`src/client/java/mchorse/bbs_mod/graphics/texture/Texture.java:164-195`** — `Texture.setFilter / Texture.setFilterMipmap / Texture.setWrap / Texture.setParameter` — _glTexParameteri-futile-vs-sampler-object_ · conf=high
  - **Доказательство:** public void setFilter(int filter) { this.filter = filter; this.setParameter(GL11.GL_TEXTURE_MAG_FILTER, filter); this.setParameter(GL11.GL_TEXTURE_MIN_FILTER, filter); } ... public void setParameter(int param, int value) { GL11.glTexParameteri(this.target, param, value); }
  - **Почему:** These methods control sampling by writing the texture object's own glTexParameteri (MIN/MAG_FILTER, WRAP_S/T, TEXTURE_MAX_LEVEL). On 1.21.5+ the new GL backend binds GL sampler objects (glBindSampler) per draw, and a bound sampler object OVERRIDES the texture's own glTexParameteri. The BBS world-render path (BillboardFormRenderer/ModelFormRenderer/ExtrudedFormRenderer -> texture.bind() + BBSShaders.getModelLayer().draw()) sets NO sampler object — getModelLayer's RenderSetup (BBSShaders.layer/reg
  - **Вердикт верификатора:** Verified directly. Texture.java:178-195 shows setFilter->setParameter->GL11.glTexParameteri(GL_TEXTURE_MAG/MIN_FILTER), setWrap->glTexParameteri(WRAP_S/T), and setFilterMipmap->setFilter + setParameter(GL_TEXTURE_MAX_LEVEL) — all per-texture-object glTexParameteri. The contrast is real: AdoptedTexture.java:121-124 attaches an EXPLICIT GpuSampler via RenderSystem.getSamplerCache().get(CLAMP,CLAMP,f
  - **Рекомендация:** Attach an explicit GpuSampler to the model/billboard/particles RenderSetup (or bind glBindSampler(0, getSamplerCache().get(...NEAREST...)) just before getModelLayer().draw()), exactly as AdoptedTexture does, so NEAREST/clamp actually takes effect on the world path. Retain setFilter/isLinear for the AdoptedTexture filter-intent feed and treat glTexParameteri here as upload-time-only.
- **`src/client/java/mchorse/bbs_mod/ui/framework/elements/input/color/UIColorPicker.java:109-128 (also pipeline/layer 85-107, call sites renderRect 736-747 and UIColorPalette 101)`** — `UIColorPicker.renderAlphaPreviewQuad (static) + getAlphaTrianglesLayer + ALPHA_TRIANGLES pipeline` — _two-phase-gui-immediate-draw-dropped_ · conf=high
  - **Доказательство:** BufferBuilder builder = Tessellator.getInstance().begin(VertexFormat.DrawMode.TRIANGLES, VertexFormats.POSITION_COLOR); ... BuiltBuffer built = builder.endNullable(); if (built != null) { getAlphaTrianglesLayer().draw(built); }
  - **Почему:** This is a self-issued mid-frame immediate RenderLayer.draw(BuiltBuffer) executed during Screen.render. Per the 1.21.6+ two-phase/deferred GUI, vanilla only composites the GuiRenderState attached to the live DrawContext; any immediate RenderLayer.draw issued mid-frame is overpainted/dropped (the exact reason the rest of the UI was migrated to context.fill/fillGradient/drawText in Batcher2D). So the alpha-gradient preview quad behind the color swatch (renderRect -> renderAlphaPreviewQuad when edit
  - **Вердикт верификатора:** Read confirms renderAlphaPreviewQuad (109-128) builds a POSITION_COLOR TRIANGLES buffer via Tessellator.getInstance().begin(...), calls builder.endNullable(), and submits it with getAlphaTrianglesLayer().draw(built) -- an immediate self-issued RenderLayer.draw(BuiltBuffer) executed mid-Screen.render. Per the 1.21.6+ two-phase/deferred GUI, vanilla only composites the GuiRenderState attached to the
  - **Рекомендация:** Replace the immediate Tessellator + RenderLayer.draw with the deferred path: build a POSITION_COLOR quad mesh and submit via Batcher2D.drawQuadMesh (GuiQuadMesh.State -> context.state.addSimpleElement), mirroring Batcher2D.box's 4-corner-gradient branch (Batcher2D.java ~321-335). Then delete the now-unused ALPHA_TRIANGLES pipeline (88-95), the alphaTrianglesLayer field (97) and getAlphaTrianglesLayer() (99-107).
- **`src/client/java/mchorse/bbs_mod/forms/renderers/ModelFormRenderer.java:50, 370`** — `ModelFormRenderer import net.minecraft.client.render.GameRenderer` — _javadoc-only-import_ · conf=medium
  - **Доказательство:** import net.minecraft.client.render.GameRenderer;  ... only use: javadoc '{@link GameRenderer}'s getXxxProgram() accessors were removed'. No code reference.
  - **Почему:** GameRenderer.getXxxProgram() accessors were removed in 1.21.5+; the import survives solely to satisfy a javadoc @link describing the removed API. Dead residue of the old shader-selection path.
  - **Вердикт верификатора:** Verified: import at line 50, and the only occurrence of GameRenderer in the file is the javadoc @link at line 370 ('{@link GameRenderer}'s getXxxProgram() accessors were removed'). grep confirms no code reference. The getXxxProgram() accessors were indeed removed in the 1.21.5+ shader-pipeline migration (consistent with this port's ground-truth that ShaderProgram/loose-uniform paths are gone). The
  - **Рекомендация:** Remove the import and downgrade the @link to plain text when cleaning up the dead getMainShader path.

### ⏭️ Намеренные заглушки незаконченных фич (deferred) — 13

- **`src/client/java/mchorse/bbs_mod/client/BBSRendering.java:446-454`** — `BBSRendering.onRenderChunkLayer (Fabric WorldRenderContextImpl prepare signature)` — _unmigrated-iris-context-rebuild_ · conf=high
  - **Доказательство:** /* ... used to hand-build a Fabric WorldRenderContextImpl via the old prepare(worldRenderer, tickCounter, blockOutlines, camera, gameRenderer, lightmap, projectionMatrix, positionMatrix, consumers, profiler, advancedTranslucency, world) signature. That API is gone ... RenderSystem.getProjectionMatrix()/MinecraftClient.getProfiler() were removed. */
  - **Почему:** Documents a 1.21.1-era render API (Fabric WorldRenderContextImpl.prepare(...) old signature, RenderSystem.getProjectionMatrix(), MinecraftClient.getProfiler()) that no longer exists on 1.21.11. The code path is already neutralized (gated behind the always-false Iris check), so it does not currently call the removed APIs — but the rebuild it describes must use the new pipeline foundation when Iris returns.
  - **Вердикт верификатора:** The cited lines are a documentation comment (not live executing code) accurately recording 1.21.1-era render APIs that no longer exist on 1.21.11: the old Fabric WorldRenderContextImpl.prepare(...) overload, RenderSystem.getProjectionMatrix(), and MinecraftClient.getProfiler(). These removals match the known engine facts for this port (RenderSystem getProjectionMatrix gone; profiler relocated). Th
  - **Рекомендация:** Keep the TODO. When reviving Iris, rebuild the context via the relocated net.fabricmc.fabric world-render API and the new WorldRenderState/SectionRenderState objects rather than the removed prepare(...) overload.
- **`src/client/java/mchorse/bbs_mod/forms/renderers/BillboardFormRenderer.java:218-221, 254`** — `BillboardFormRenderer.renderQuad (texture.setFilterMipmap)` — _setfiltermipmap-vs-sampler_ · conf=high
  - **Доказательство:** BBSModClient.getTextures().bindTexture(texture); texture.bind(); texture.setFilterMipmap(this.form.linear.get(), this.form.mipmap.get()); ... layer.draw(built); texture.setFilterMipmap(false, false);  (Texture.setFilterMipmap -> setFilter -> GL11.glTexParameteri(MIN/MAG)).
  - **Почему:** The quad is drawn through BBSShaders.getModelLayer() whose pipeline declares Sampler0; on 1.21.5+ the engine binds a GL sampler object (glBindSampler) for that unit, which OVERRIDES the texture's own glTexParameteri(GL_TEXTURE_MIN/MAG_FILTER). So the per-texture linear/nearest+mipmap set here is futile via the RenderLayer path and the form.linear/form.mipmap options have no effect.
  - **Вердикт верификатора:** Verified at 216-221: layer=shader.get() (BBSShaders::getModelLayer), bindTexture, texture.bind(), texture.setFilterMipmap(form.linear, form.mipmap); then layer.draw(built) at 251; reset setFilterMipmap(false,false) at 254. Per this port's engine ground-truth, on 1.21.5+ a pipeline declaring Sampler0 binds a GL sampler object (glBindSampler) for that unit which overrides the texture's own glTexPara
  - **Рекомендация:** Apply filtering/mipmap via the layer's sampler configuration (RenderSetup / sampler object) rather than raw glTexParameteri on the texture, once the Sampler0 wiring lands. Until then this call pair is inert.
- **`src/client/java/mchorse/bbs_mod/forms/renderers/FramebufferFormRenderer.java:118-128, 161-208, 14, 28`** — `FramebufferFormRenderer.renderModel / renderQuad (Supplier<ShaderProgram> shader)` — _shaderprogram-plumbing_ · conf=high
  - **Доказательство:** Supplier<ShaderProgram> shader = () -> null; this.renderModel(framebuffer.getMainTexture(), format, shader, ...); ... renderQuad(VertexFormat format, Texture texture, Supplier<ShaderProgram> shader, ...) { /* shader never referenced */ }
  - **Почему:** The Supplier<ShaderProgram> is created as a constant () -> null and threaded through renderModel into renderQuad where it is never read. ShaderProgram / GameRenderer.getXxxProgram() no longer back a usable program on 1.21.11 (shaders live in RenderPipeline/RenderLayer). The parameter, the local, and the net.minecraft.client.gl.ShaderProgram + java.util.function.Supplier imports exist only to satisfy the removed-API shape; they carry no behaviour.
  - **Вердикт верификатора:** Confirmed. Line 123 `Supplier<ShaderProgram> shader = () -> null;` is a constant stub, threaded through renderModel (line 128 signature, passed on at line 158) into renderQuad (line 161 signature) where the `shader` parameter is never referenced anywhere in the body (the quad is built into a Tessellator BufferBuilder and discarded). Imports `net.minecraft.client.gl.ShaderProgram` (line 14) and `ja
  - **Рекомендация:** When the framebuffer composite is rebuilt on RenderLayer/RenderPipeline, drop the Supplier<ShaderProgram> parameter and the ShaderProgram import; submit the built quad via a textured RenderLayer.draw. No analogue exists in the new engine.
- **`src/client/java/mchorse/bbs_mod/forms/renderers/FramebufferFormRenderer.java:84-115`** — `FramebufferFormRenderer.renderBodyParts` — _removed-gl-state-and-lighting_ · conf=high
  - **Доказательство:** /* TODO(1.21.11 render): RenderSystem.shaderLightDirections / setShaderLights(Vector3f,Vector3f) / getProjectionMatrix / setProjectionMatrix / getModelViewStack were removed ... */ GL30.glCullFace(GL30.GL_FRONT); ...  GL30.glCullFace(GL30.GL_BACK);
  - **Почему:** The original saved/replaced the two shader light directions, projection matrix and model-view stack while rendering inner forms into an ortho framebuffer, then restored them. Those APIs (setShaderLights(Vector3f,Vector3f), get/setProjectionMatrix, getModelViewStack) were removed in the 1.21.5 GPU rewrite (lighting is a Lighting UBO / GpuBufferSlice, projection is a dynamic RenderSystem uniform). The port keeps only the raw glCullFace(GL_FRONT/GL_BACK) and renders the inner forms with whatever li
  - **Вердикт верификатора:** Confirmed. The TODO at 84-90 documents that `RenderSystem.shaderLightDirections / setShaderLights(Vector3f,Vector3f) / getProjectionMatrix / setProjectionMatrix / getModelViewStack` were removed by the 1.21.5 GPU rewrite (lighting now a GpuBufferSlice/Lighting UBO, projection a dynamic RenderSystem uniform) — consistent with the ground-truth fact that setupLevelDiffuseLighting/loose shader-light s
  - **Рекомендация:** Re-implement the flat front-light + ortho-projection bracket via RenderSystem.setShaderLights + a temporary projection uniform when the framebuffer path is rebuilt on the new pipeline foundation.
- **`src/client/java/mchorse/bbs_mod/forms/renderers/ModelFormRenderer.java:377-380, 361, 655, 716-720, 382-413`** — `ModelFormRenderer.getMainShader / renderModel(program) / render3D shader plumbing` — _shaderprogram-supplier-dead_ · conf=high
  - **Доказательство:** private Supplier<ShaderProgram> getMainShader(ModelInstance model){ return () -> null; }  ...  Supplier<ShaderProgram> shader = this.getShader(context, mainShader, () -> null); ... this.renderModel(context.entity, shader, ...). ModelInstance.render(MatrixStack, Supplier<ShaderProgram> program, ...) never calls program.get() (verified: render() picks RenderLayer/RenderPipeline itself at ModelInstance.java:400-423).
  - **Почему:** The whole Supplier<ShaderProgram> path is 1.21.1-era residue. ShaderProgram.bind()/loose-uniform fields are gone in 1.21.5+; getMainShader always yields a null supplier and ModelInstance.render ignores the program argument entirely, so every ShaderProgram value threaded through getMainShader -> getShader -> renderModel -> model.render is computed-but-unused dead code. getShader also passes picking.get() (==null) into setupTarget, which ignores it.
  - **Вердикт верификатора:** Verified at ModelFormRenderer.java:377-380 getMainShader returns `() -> null`. The Supplier<ShaderProgram> is threaded through renderModel(382) into model.render(413). Confirmed at ModelInstance.java:376-425 that render() never calls program.get(): the immediate/VAO branches each select RenderLayer/RenderPipeline themselves (getModelLayer/entityCutoutNoCull/BBSPickerRenderer), and the picker path 
  - **Рекомендация:** When the model render path is rebuilt on RenderPipeline, drop the Supplier<ShaderProgram> parameter from renderModel/ModelInstance.render and delete getMainShader; select the picker pipeline + BBSPicker UBO directly. Until then it is an inert no-op.
- **`src/client/java/mchorse/bbs_mod/forms/renderers/TrailFormRenderer.java:63-77, 249, 265-333`** — `TrailFormRenderer.TRAIL_PIPELINE / getTrailLayer / render3D (strip)` — _pipeline-missing-sampler_ · conf=high
  - **Доказательство:** private static final RenderPipeline TRAIL_PIPELINE = RenderPipelines.register(RenderPipeline.builder(RenderPipelines.POSITION_TEX_COLOR_SNIPPET)...withVertexFormat(VertexFormats.POSITION_TEXTURE, VertexFormat.DrawMode.QUADS)...); ... BBSModClient.getTextures().bindTexture(this.form.texture.get());
  - **Почему:** The trail strip pipeline is seeded from POSITION_TEX_COLOR_SNIPPET but declares no sampler, while the original bound the trail texture globally via getTextures().bindTexture(...) and sampled it through getPositionTexProgram. On 1.21.11 samplers must be declared by the pipeline; the global bindTexture at line 249 therefore feeds a texture the pipeline never reads, so the trail strip renders untextured (and the vertex format omits color). The bindTexture call is effectively dead for this draw.
  - **Вердикт верификатора:** Confirmed. TRAIL_PIPELINE (lines 69-77) is built from POSITION_TEX_COLOR_SNIPPET with vertex format VertexFormats.POSITION_TEXTURE/QUADS and declares no sampler. The strip buffer (lines 265-322) writes only position+texture (no color attr, mismatching the snippet's color expectation). The texture is bound globally via `BBSModClient.getTextures().bindTexture(this.form.texture.get())` (line 249) but
  - **Рекомендация:** Add a dedicated textured position-tex pipeline (declared Sampler0 + per-draw texture binding, matching vertex format) and remove the futile global bindTexture once the GpuTextureView bridge lands.
- **`src/client/java/mchorse/bbs_mod/ui/film/UISubtitleRenderer.java:52-53, 193`** — `UISubtitleRenderer.renderSubtitles (program/supplier -> texturedBox)` — _ignored-shader-supplier-bridge_ · conf=high
  - **Доказательство:** RenderPipeline program = BBSShaders.getSubtitlesProgram(); Supplier<RenderPipeline> supplier = () -> program; ... batcher.texturedBox(supplier, texture.id, ...) -- Batcher2D.texturedBox(Supplier<RenderPipeline>,...) is @Deprecated and explicitly ignores the supplier ('The Supplier (pipeline selector) is ignored', Batcher2D.java:601-606), always routing through GUI_TEXTURED.
  - **Почему:** The subtitle blur pipeline (getSubtitlesProgram) is fetched and wrapped in a Supplier purely to feed an argument the 1.21.5+ bridge discards, so the blur shader / SubtitlesInfo UBO is never applied. This is a deliberate, well-documented deferral (the long TODO at lines 179-189 describes the missing manual RenderPass dispatch for the custom subtitle shader), so the program/supplier allocation is intentional placeholder residue rather than a regression.
  - **Вердикт верификатора:** Verified. Line 52 fetches RenderPipeline program = BBSShaders.getSubtitlesProgram(); line 53 wraps it in Supplier<RenderPipeline> supplier; line 193 passes supplier to batcher.texturedBox(Supplier,...). Batcher2D.java:601-606 confirms that overload is @Deprecated and the supplier is explicitly ignored ('The Supplier (pipeline selector) is ignored'), always routing through drawAdoptedGlTexture/GUI_
  - **Рекомендация:** Leave until the 'revive GUI custom shaders' work lands; then replace the ignored texturedBox(Supplier,...) call with a manual RenderPass binding getSubtitlesProgram() + SubtitlesInfo UBO + Sampler0. Until then the supplier/program allocation could be dropped to avoid implying the blur is active.
- **`src/client/java/mchorse/bbs_mod/ui/framework/elements/input/multilink/UIMultiLinkEditor.java:222-238 (bridge in Batcher2D.java:601-606)`** — `UIMultiLinkEditor.renderCanvasFrame (finalProgram / BBSShaders.getMultilinkProgram)` — _ignored-supplier-pipeline-multilink_ · conf=high
  - **Доказательство:** final RenderPipeline finalProgram = needsMultLinkShader ? BBSShaders.getMultilinkProgram() : RenderPipelines.GUI_TEXTURED; context.batcher.texturedBox(() -> finalProgram, texture.id, child.color, ...);  -- and Batcher2D.texturedBox(Supplier,...) is @Deprecated: 'The Supplier (pipeline selector) is ignored. ... always uses GUI_TEXTURED'.
  - **Почему:** The per-draw pipeline Supplier is computed (getMultilinkProgram() when pixelate>1 or erase) but texturedBox(Supplier,...) ignores it and always routes through the AdoptedTexture->GUI_TEXTURED bridge. So getMultilinkProgram()/the MultilinkInfo UBO/Sampler3 erase-atlas are never bound and the pixelate/erase filter is not applied (children draw unfiltered). The computed finalProgram branch is effectively dead at the dispatch boundary. This is explicitly documented at both the call site and the brid
  - **Вердикт верификатора:** Verified: finalProgram is computed as BBSShaders.getMultilinkProgram() when needsMultLinkShader (child.pixelate > 1 || child.erase) else GUI_TEXTURED (234-236), then passed as a Supplier to context.batcher.texturedBox(() -> finalProgram, ...) (238). Batcher2D.texturedBox(Supplier,...) is @Deprecated (601) and its body ignores the supplier entirely — it calls drawAdoptedGlTexture(...) (605), routin
  - **Рекомендация:** Defer with the documented 'revive custom GUI shaders' task: a real custom two-phase-GUI draw (manual RenderPass binding getMultilinkProgram(), upload MultilinkInfo std140 UBO, bind Sampler0 + Sampler3, composite via a SpecialGuiElementRenderer). When done, drop getMultilinkLayer() if still unused.
- **`src/client/java/mchorse/bbs_mod/ui/framework/elements/utils/Batcher2D.java:591-606`** — `Batcher2D.texturedBox(Supplier<RenderPipeline>,int,...)` — _ignored-shader-supplier-shim_ · conf=high
  - **Доказательство:** @Deprecated public void texturedBox(Supplier<RenderPipeline> shader, int texture, int color, float x, float y, float w, float h, float u1, float v1, float u2, float v2, int textureW, int textureH) {     /* TODO(1.21.11 render): verify at runtime. The Supplier (pipeline selector) is ignored. */     this.drawAdoptedGlTexture(texture, color, x, y, w, h, u1, v1, u2, v2, textureW, textureH); }
  - **Почему:** The 1.21.1-era per-draw shader-program / pipeline supplier no longer functions in the 1.21.5+ engine: the immediate RenderLayer.draw path cannot carry a custom UBO and the two-phase GUI only has GUI_TEXTURED. This shim keeps the 1.21.1 signature for source compatibility but silently DROPS the supplied pipeline (always GUI_TEXTURED), so callers that depended on a custom pipeline's per-draw uniforms render unfiltered. Live callers depend on this: UIMultiLinkEditor.java:238 passes BBSShaders.getMul
  - **Вердикт верификатора:** Re-read lines 591-606: @Deprecated overload accepting Supplier<RenderPipeline> shader that is silently ignored - body just calls drawAdoptedGlTexture (always GUI_TEXTURED). The per-draw shader-program/pipeline supplier is a 1.21.1-era API that no longer functions: the immediate RenderLayer.draw path cannot carry a custom UBO and the two-phase GUI only exposes GUI_TEXTURED. Live caller confirmed at
  - **Рекомендация:** Keep as a documented bridge; migrate to a real custom two-phase-GUI draw (SpecialGuiElementRenderer + manual RenderPass binding the custom pipeline + UBO) so the supplied pipeline is honored. Do not delete while multilink/subtitle pass a custom pipeline.
- **`src/client/java/mchorse/bbs_mod/cubic/ik/ModelIKDebug.java:139-149, 231-253, 312-335`** — `ModelIKDebug.flush / renderStencil / drawChain` — _immediate-renderlayer-draw-verify_ · conf=medium
  - **Доказательство:** BufferBuilder builder = Tessellator.getInstance().begin(VertexFormat.DrawMode.TRIANGLES, VertexFormats.POSITION_COLOR); ... private static void flush(BufferBuilder builder, RenderLayer layer){ BuiltBuffer built = builder.endNullable(); if (built != null){ /* TODO(1.21.11 render): verify at runtime ... */ layer.draw(built); } }
  - **Почему:** These are self-issued mid-frame immediate draws (Tessellator.begin -> RenderLayer.draw(BuiltBuffer)). Per the 1.21.6+ two-phase GUI, such draws are silently dropped in the GUI phase. ModelIKDebug.render / renderStencil are invoked from ModelFormRenderer in the WORLD/entity pass (ModelIKDebug.render at line 417 of ModelFormRenderer, renderStencil from updateStencilMap), where this immediate path is the correct in-world pattern (same as ModelVAORenderer/Draw). So it is NOT broken for the in-world 
  - **Вердикт верификатора:** Verified the flush() path (Tessellator.begin -> builder.endNullable() -> layer.draw(built)) at lines 139-149, and the Tessellator.begin calls in renderStencil (231) and drawChain (312, 324). Confirmed the call sites: ModelFormRenderer.java:417 invokes ModelIKDebug.render in the world/entity pass (renderCoolStuff region), and ModelFormRenderer.java:755 invokes renderStencil from the stencil/picking
  - **Рекомендация:** Confirm at runtime that the IK debug overlay only flushes during world/entity/stencil passes (where RenderLayer.draw is honoured) and never during a two-phase GUI model preview; if it can run in a GUI preview, route it through the live DrawContext/GuiRenderState. Otherwise the migration is correct — clear the TODO once verified.
- **`src/client/java/mchorse/bbs_mod/forms/renderers/BillboardFormRenderer.java:216-252`** — `BillboardFormRenderer.renderQuad (getModelLayer draw)` — _sampler0-unbound-modellayer_ · conf=medium
  - **Доказательство:** RenderLayer layer = shader.get(); BBSModClient.getTextures().bindTexture(texture); texture.bind(); ... BuiltBuffer built = builder.endNullable(); if (built != null){ layer.draw(built); }  with in-code TODO: 'the bound texture (Sampler0) is currently fed via the old global texture binding; the BBS layer's sampler wiring ... so the billboard texture may not sample until then.'
  - **Почему:** getModelLayer()'s pipeline declares Sampler0 but RenderLayer.draw(BuiltBuffer) does not bind the form texture into Sampler0 (the immediate RenderLayer path only carries engine builtins + pipeline samplers it sets itself). The old RenderSystem.setShaderTexture/global bind no longer feeds the pipeline sampler, so the shaded billboard may render untextured until the GpuTextureView/sampler bridge lands.
  - **Вердикт верификатора:** Verified at 216-252: layer = BBSShaders::getModelLayer.get(); texture bound via the old BBSModClient.getTextures().bindTexture + texture.bind(); built buffer drawn via layer.draw(built) at 251. The in-code TODO at 244-246 explicitly states Sampler0 is fed via the old global texture binding and the layer's sampler wiring (replacing RenderSystem.getShaderTexture) is pending, so the billboard may not
  - **Рекомендация:** Bridge the bound model texture into the layer's Sampler0 via a GpuTextureView (AdoptedTexture) as part of the pipeline-foundation work, mirroring the picker Sampler0 bridge used in ModelFormRenderer.render3D (lines 722-729).
- **`src/client/java/mchorse/bbs_mod/particles/emitter/ParticleEmitter.java:479-535`** — `ParticleEmitter.render / ParticleEmitter.bindTexture` — _raw-gl-bind-feeding-renderlayer-sampler0_ · conf=medium
  - **Доказательство:** this.bindTexture(); BufferBuilder builder = Tessellator.getInstance().begin(...); ... layer.draw(built);  // and the in-code TODO: "The per-emitter texture binding (bindTexture above, old GL-style) still needs to be wired to the layer's Sampler0; until then the geometry is built faithfully but may sample the wrong texture."
  - **Почему:** bindTexture() routes through TextureManager.bindTexture -> Texture.bind -> GlStateManager._bindTexture (a global active-texture GL bind, the migrated replacement for the removed RenderSystem.setShaderTexture). On 1.21.5+ the RenderLayer/RenderPipeline path binds its own Sampler0 via the pipeline, so the per-emitter texture set with a global GL bind is not guaranteed to be the one sampled — the same Sampler0-binding gap called out for billboards. getParticlesLayer()'s own javadoc even relies on t
  - **Вердикт верификатора:** Verified at lines 479-535. render() calls this.bindTexture() (line 497) then builds the buffer and submits via layer.draw(built) (line 520). bindTexture() (530-535) routes through TextureManager.bindTexture -> Texture.bind(unit). Confirmed in TextureManager.java line 99-108 that bindTexture is a fallback for the removed RenderSystem.setShaderTexture ('RenderSystem.setShaderTexture(unit, id) was re
  - **Рекомендация:** Migrate the per-emitter texture to be supplied through the RenderLayer's Sampler0 via the Texture->GpuTextureView bridge (same fix the billboard/texture work unit needs) rather than a global GlStateManager._bindTexture call. Until then in-world/preview particles can sample the wrong/last-bound texture.
- **`src/client/java/mchorse/bbs_mod/ui/forms/UIFormList.java:294-298`** — `UIFormList.render` — _removed-gui-diffuse-lighting_ · conf=low
  - **Доказательство:** /* TODO(1.21.11 render): DiffuseLighting.enableGuiDepthLighting()/disableGuiDepthLighting() were removed by the GPU-pipeline rewrite (GUI item lighting is now driven by the RenderPipeline). The explicit enable/disable around the child render is dropped; verify item-form icons still light correctly at runtime. */ super.render(context);
  - **Почему:** The 1.21.1 code bracketed the form-list child render with DiffuseLighting.enableGuiDepthLighting()/disableGuiDepthLighting() so 3D item-form icons were lit. Those static calls no longer exist on 1.21.11 (GUI item lighting moved into the RenderPipeline). The calls are already removed (only a comment remains -- no offending call left to compile), but item-form icons in the list may render with wrong/default lighting until the new lighting path is confirmed. Reported as a deprecated-API render gap 
  - **Вердикт верификатора:** Verified: lines 294-298 are only a TODO(1.21.11 render) comment immediately before super.render(context); the DiffuseLighting.enableGuiDepthLighting()/disableGuiDepthLighting() bracket calls are gone — no offending call remains to compile. Corroborated that the API removal is real: every other DiffuseLighting reference in the tree (FormRenderer.java:58, UIModelRenderer.java:82-89, ItemFormRenderer
  - **Рекомендация:** Verify at runtime that item-form icons in the form list are correctly lit; if dark/flat, supply GUI item lighting via the new RenderPipeline path (as the in-tree list-preview lighting does). No code to delete.

## Покрытие по областям

| Область | Сырых | Подтв. | Переклас. | Отклон. | +verify |
|---|---|---|---|---|---|
| shaders-glsl | 9 | 9 | 0 | 0 | 0 |
| client-core | 5 | 5 | 0 | 0 | 0 |
| cubic-render-vao | 4 | 4 | 0 | 0 | 0 |
| cubic-render-other | 5 | 5 | 0 | 0 | 0 |
| cubic-model | 4 | 4 | 0 | 0 | 0 |
| forms-renderers-A | 9 | 9 | 0 | 0 | 0 |
| forms-renderers-B | 8 | 8 | 0 | 0 | 0 |
| graphics-core | 3 | 3 | 0 | 0 | 0 |
| graphics-line | 1 | 1 | 0 | 0 | 0 |
| graphics-texture | 1 | 1 | 0 | 0 | 0 |
| batcher2d | 4 | 4 | 0 | 0 | 0 |
| ui-framework-render | 1 | 1 | 0 | 0 | 1 |
| mixin-render | 5 | 5 | 0 | 0 | 0 |
| client-renderer-entity-item | 6 | 6 | 0 | 0 | 0 |
| picker | 3 | 3 | 0 | 0 | 0 |
| particles | 4 | 4 | 0 | 0 | 0 |
| film | 2 | 2 | 0 | 0 | 0 |
| ui-film-render | 7 | 7 | 0 | 0 | 0 |
| utils-matrix | 2 | 2 | 0 | 0 | 0 |
| ui-misc-render | 5 | 4 | 1 | 0 | 1 |

---

# Слияние `1.21.1` → `1.21.11` (2026-06-20): временные заглушки

Влиты 140 коммитов фич-дельты ветки `1.21.1` (рефактор ИК «1-в-1 Blender», физика костей, anchor-gizmo, фиксы гизмо/биллборда, лимит FPS рендера и т.д.) в порт `1.21.11`. Конфликт-файлов: 21 (20 content + 1 modify/delete); вторая волна — авто-смёрженный код фич на удалённых API: ещё 7 файлов. **Сборка зелёная** (`./gradlew build` → `bbs-2.3-1.21.11.jar`). Версия мода поднята 2.2.1 → **2.3** (из `1.21.1`).

Принцип разрешения: сторона `HEAD` = портированный рендер 1.21.11 (RenderPipeline/RenderLayer, 2D GUI Matrix3x2fStack) — **сохранена, должна компилироваться**; сторона `1.21.1` = новая фича на старом API (ShaderProgram/GlUniform/VertexBuffer/RenderSystem.*/Matrix4f GUI) — **интегрирована, если version-agnostic; иначе отложена заглушкой** с маркером `// TODO(1.21.11 render merge)`.

Точка отката: backup-ветка `1.21.11-premerge-backup` @ `09485317`.

## ✅ Фичи 1.21.1, ИНТЕГРИРОВАННЫЕ (работают, без заглушки)

- **`ui/Keys.java`** — орбита-телепорт перевязана `Z` → `C` + добавлен `FILM_CONTROLLER_ATTACH_ORBIT` (`Z`+`Shift`).
- **`ui/film/controller/FilmEditorController.java`** — anchor-gizmo: `.anchorGizmo(anchorGizmo, controller.getAnchorSpace())` встроен в билдер (tick-доступ оставлен 1.21.11: `getRenderTickCounter().getTickProgress`).
- **`ui/forms/UIFormList.java`** — scroll-to-selected (`pendingScrollToSelected`/`scrollToSelectedForm()`) сохранён.
- **`ui/forms/editors/utils/UIPickableFormRenderer.java`** — гард «скрыть гизмо удержанием» (`!UIBaseMenu.isHideGizmoHeld()`) обёрнут вокруг существующего HEAD-вызова `Gizmo.INSTANCE.renderStencil`.
- **`BBSModClient.java`** — цветные подписи порядка хоткеев (`.colors(Colors.A100 | Colors.RED, ...)`): добавлен `import ...utils.colors.Colors`.
- **`ui/film/FilmEditorUserActivity.java`** — реструктуризация цикла опроса клавиш (`continue` на `GLFW_KEY_UNKNOWN`); вызов адаптирован к 1.21.11: `InputUtil.isKeyPressed(mc.getWindow(), key)` (overload c `Window`, не `long`).
- **`ui/model_blocks/UIModelBlockEditorMenu.java`** — оставлен HEAD (прямой set `orbitCameraController.camera` + `getEntityPos()`), 1.21.1-рефактор `new Camera()`/`orbit.setup()` отброшен (имя метода `getPos()` устарело).

## ⏳ ОТЛОЖЕНО заглушкой (фича 1.21.1 потеряна до ре-порта на pipeline)

### Рендер моделей / VAO
- **`cubic/ModelInstance.java:~411,~474` + `cubic/render/CubicVAORenderer.java:~31,~66`** — _per-mesh/per-material резолвер текстур BOBJ_. 1.21.1 протягивал `ShaderProgram` + `Function<String,Link> textureResolver` и биндил `BBSModClient.getTextures().bindTexture(textureResolver.apply(mesh.name))` на каждый меш/материал. HEAD рисует все меши без раздельной текстуры. **Ре-порт:** per-mesh bind через pipeline RenderLayer.
- **`forms/renderers/ModelFormRenderer.java:~457`** — _ModelPhysicsDebug.render в редакторе форм_ (gated `enabled` + `form.physics` MapType). Вызов отброшен. Stencil-вариант `ModelPhysicsDebug.renderStencil` (строка ~786) остаётся подключённым (но сам класс — заглушка, см. ниже).
- **`cubic/physics/ModelPhysicsDebug.java`** — **ВЕСЬ НОВЫЙ ФАЙЛ-ФИЧА заглушён в no-op** (`render`/`renderStencil` пустые). Это physics-overlay (провода-цепи через шарики-суставы, пикинг attach-костей) целиком на старом immediate API (`Tessellator.begin`/`BufferRenderer.drawWithGlobalProgram`/`RenderSystem.*`/`getPositionColorProgram`). **Ре-порт:** на тот же pipeline-flush, что и `ModelIKDebug` (`getLinesLayer()`/`getTrisLayer()`). Полный оригинал — в git-истории (ветка `1.21.1`).

### ИК-дебаг
- **`cubic/ik/ModelIKDebug.java:~351,~373`** — _визуализация pole-target ИК_: доп. линия `POLE` от `pts[1]` к полюсу + орб на полюсе. Отброшено (HEAD `flush(getLinesLayer()/getTrisLayer())` оставлен). `pole`/`POLE` ещё в коде, не используются. **Ре-порт:** до-эмитить 2 примитива во flush.
- **`cubic/ik/ModelIKDebug.java:~232`** — `RenderSystem.disableDepthTest()`+`setShader` (теперь pipeline-encoded в слое).

### Гизмо (`ui/utils/Gizmo.java`)
- **VBO-кэш колец вращения** (`updateVbos`/`drawCachedRing`/`drawCachedRingBillboard` + поля `rotateRingVbo`/`rotateStencilRingVbo`/`rotateSphereVbo`, тип `VertexBuffer`) — перф-кэш геометрии. HEAD рисует кольца immediate каждый кадр (работает, не кэшируется). **Ре-порт:** кэш на RenderPipeline/RenderLayer.
- **Заморозка вращения на drag** (`bakedRotationMatrix` + `applyBakedRotation`/`bakeRotation`/`isBakingRotation`) — фиксировала ориентацию кольца на момент захвата. **Заглушено no-op** (поле `hasBakedRotation` + пустые методы), внешний `UIPropTransform.bakeRotation()` подключён. **Ре-порт:** strip/re-apply под раздельные visual/stencil камера-конвенции 1.21.11.
- **`renderSphereHighlight(UIContext, Matrix4f, Area)`** — авто-смёрженный 1.21.1-код ВНЕ конфликт-хунков (сломал бы сборку): рисовал `rotateSphereVbo` в FBO + композитил через `GlUniform` на `ShaderProgram` picker_preview. **Заглушено no-op**, сигнатура сохранена (`GizmoInteraction`/`UIPickableFormRenderer`/`UIFilmController`/`UIModelBlockPanel` подключены). **Ре-порт:** off-screen highlight pass через `BBSPickerRenderer`.
- **`Gizmo.java:~893`** — `BBSSettings.rotate3dSphereColor` **удалён в 1.21.1** («убран цвет 3д сферы», сфера там невидима). HEAD рисует слабый тинт сферы → фоллбэк на нейтральный `0x33FFFFFF`. **Ре-порт:** решить — невидимая сфера (как 1.21.1) или вернуть настройку.
- _Примечание:_ per-handle `active`-куллинг при drag сохранён для планов/scale-кубов/screen-куба; бары в stencil-пассе рисуются безусловно (HEAD-faithful, лишь лишние id в off-screen pick-текстуре).

### Запись / превью фильма
- **`mixin/client/RenderTickCounterMixin.java:~44`** — _лимит частоты кадров рендера_ (`BBSSettings.videoLimitFrameRate`: держать кадр до набега wall-clock, `canRender=false`, `setReturnValue(0)`). На удалённых полях `tickDelta`/`lastFrameDuration`/`prevTimeMillis`. **Ре-порт:** на shadowed `tickProgress`/`dynamicDeltaTicks`/`lastTimeMillis`; `videoLimitFrameRate`/`canRender`/`lastFrameTime` ещё есть.
- **`client/BBSRendering.java:~444`** — _HiDPI экспорт-даунскейл (суперсэмплинг)_: 1.21.1 `glBlitFramebuffer`-ил нативный кадр в export-`Framebuffer` размера `getVideoWidth()/getVideoHeight()` с `GL_LINEAR`. HEAD снимает на нативном размере (`glCopyTexSubImage2D`, без ресайза). **Ре-порт:** pipeline-side downscale blit/resolve.
- **`film/BaseFilmController.java:~329`** — `RenderSystem.getProjectionMatrix()` удалён → FOV-детект ortho/perspective отложен, фоллбэк на `BBSSettings.getFov()` (distance-scale гизмо работает). `~336,~388` — `RenderSystem.enableDepthTest()` удалён (depth pipeline-encoded).
- **`ui/film/UIFilmPreview.java:~472`** — кросс-хэйр ориентации камеры в углу превью (уже был штатной заглушкой порта; остаётся заглушкой). `RenderSystem.renderCrosshair/applyModelViewMatrix` + GUI `Matrix4f peek` удалены.

### UI
- **`ui/dashboard/textures/UIPixelsEditor.java:~1010` (`renderSelection`)** — _marching-ants контур выделения_ через кастомный GLSL `selection` (`BBSShaders.getSelectionProgram` + `GlUniform` Phase/Scale). **Заглушено** (логика выделения цела, контур не рисуется). **Ре-порт:** на pipeline (см. «revive GUI custom shaders»).
- **`forms/renderers/FramebufferFormRenderer.java` (`renderInUI`)** — превью framebuffer-формы в UI. HEAD-версия порта была **пустой** `{}`; 1.21.1-тело (3D MatrixStack через `getMatrices()` — теперь 2D — + `RenderSystem.depthFunc`) авто-влилось и сломало сборку → возвращена заглушка (показывается иконка камеры для пустого состояния). **Ре-порт:** 2D→3D GUI-матричный мост + pipeline depth.
- **`ui/forms/UIFormList.java:~347`** — одиночный вызов `DiffuseLighting.disableGuiDepthLighting()` отброшен (метод удалён в 1.21.11; GUI-освещение pipeline-driven). Scroll-to-selected рядом — сохранён.

### Инфраструктура / удалённое
- **`client/BBSShaders.java:~226`** — старая система `ShaderProgram` + `ProxyResourceFactory` + `get*Program()` отброшена целиком. **Инфра, не пользовательская фича** — полностью заменена RenderPipeline/RenderLayer. Ре-порт не нужен.
- **`mixin/client/EntityRenderDispatcherMixin.java`** — modify/delete: порт удалил (морфы сущностей переехали на `LivingEntityRendererMixin` / `EntityRenderState`). Оставлен **удалённым**; 1.21.1-версия (WrapOperation на `EntityRenderer.render`) отброшена. Ре-порт не нужен, пока механизм `EntityRenderState` не регрессирует.
